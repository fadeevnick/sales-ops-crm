# Implementation Status

## Summary

- Product selected: B2B Sales Operations CRM with approvals
- Business requirements: done
- User journeys: drafted
- Functional requirements: drafted
- Architecture: drafted
- Tech stack: drafted
- MVP roadmap: drafted
- Implementation guide: drafted
- Core flow prototypes: drafted
- Implementation-ready design details: drafted
- Phase 1 backlog: drafted
- Phase 1 file-level plan: drafted
- Schema draft for Phase 1-2: drafted
- Test matrix for Phase 0-2: drafted
- Runtime checklists: drafted
- Phase 2 file-level plan: drafted
- Phase 3 file-level plan: drafted
- MVP cut register: drafted
- ADR-001 metadata storage boundary: drafted
- ADR-002 shell/auth boundary: drafted
- ADR-003 opportunity stage transition model: drafted
- ADR-004 approval-request-vs-opportunity separation: drafted
- ADR-005 access enforcement layer: drafted
- Phase 1 coding slice planning note: drafted
- Phase 1 coding slice review note: drafted
- Phase 1 second coding slice planning note: drafted
- V2 Phase 1 identity hardening spec: drafted
- V2 Phase 1 identity hardening acceptance checklist: drafted
- Phase 1 third coding slice planning note: drafted
- Phase 2 first coding slice planning note: drafted
- V3 Phase 2 CRM core spec: drafted
- V3 Phase 2 CRM core acceptance checklist: drafted
- Phase 2 second coding slice planning note: drafted
- Phase 2 third coding slice planning note: drafted
- Phase 2 fourth coding slice planning note: drafted
- Phase 2 fifth coding slice planning note: drafted
- Phase 2 sixth coding slice planning note: drafted
- Phase 2 seventh coding slice planning note: drafted
- Phase 2 eighth coding slice planning note: drafted
- Phase 2 ninth coding slice planning note: drafted
- Phase 2 tenth coding slice planning note: drafted
- Phase 2 eleventh coding slice planning note: drafted
- Phase 2 twelfth coding slice planning note: drafted
- Phase 3 first coding slice planning note: drafted
- Phase 3 second coding slice planning note: drafted
- Phase 3 third coding slice planning note: drafted
- Phase 3 fourth coding slice planning note: drafted
- Phase 3 fifth coding slice planning note: drafted
- Phase 3 sixth coding slice planning note: drafted
- Phase 3 seventh coding slice planning note: drafted
- Phase 3 eighth coding slice planning note: drafted
- Phase 3 ninth coding slice planning note: drafted
- Phase 3 tenth verification slice planning note: drafted
- Phase 4 file-level plan: drafted
- Phase 4 first coding slice planning note: drafted
- Phase 4 twelfth coding slice planning note: drafted
- Phase 4 thirteenth coding slice planning note: drafted
- Phase 4 completion review note: drafted
- Phase 5 first coding slice planning note: drafted
- Phase 5 second coding slice planning note: drafted
- First Phase 5 coding slice implementation: accepted on backend/frontend/API sanity check
- Phase 5 third coding slice planning note: drafted
- Second Phase 5 coding slice implementation: accepted on backend/frontend/API sanity check
- Phase 5 fourth coding slice planning note: drafted
- Third Phase 5 coding slice implementation: accepted on backend/frontend/API sanity check
- Phase 5 fifth coding slice planning note: drafted
- Fourth Phase 5 coding slice implementation: accepted on backend/API sanity check
- Phase 5 sixth coding slice planning note: drafted
- Fifth Phase 5 coding slice implementation: accepted on backend/API sanity check
- Phase 5 completion review note: drafted
- Phase 6 first coding slice planning note: drafted
- Sixth Phase 5 coding slice implementation: accepted on frontend/API sanity check
- Phase 5 completion decision: accepted with deferred enhancements
- Phase 6 second coding slice planning note: drafted
- First Phase 6 coding slice implementation: accepted on backend/API sanity check
- Second Phase 6 coding slice implementation: accepted on backend/API sanity check
- Phase 6 third coding slice planning note: drafted
- Third Phase 6 coding slice implementation: accepted on backend/API sanity check
- Phase 6 fourth coding slice planning note: drafted
- Fourth Phase 6 coding slice implementation: accepted on backend/API sanity check
- Phase 6 fifth coding slice planning note: drafted
- Fifth Phase 6 coding slice implementation: accepted on frontend build/browser sanity check
- Phase 6 sixth coding slice planning note: drafted
- Sixth Phase 6 coding slice implementation: accepted on backend/API sanity check
- Phase 6 seventh coding slice planning note: drafted
- Seventh Phase 6 coding slice implementation: accepted on frontend build/browser sanity check
- Phase 6 eighth coding slice planning note: drafted
- Eighth Phase 6 coding slice implementation: accepted on backend/API sanity check
- Phase 6 ninth coding slice planning note: drafted
- Ninth Phase 6 coding slice implementation: accepted on frontend build/browser sanity check
- Phase 6 tenth coding slice planning note: drafted
- Tenth Phase 6 coding slice implementation: accepted on backend/API sanity check
- Phase 6 eleventh coding slice planning note: drafted
- Eleventh Phase 6 coding slice implementation: accepted on frontend build/browser sanity check
- Phase 6 completion review note: drafted
- Phase 6 completion decision: accepted with deferred enhancements
- Phase 7 first coding slice planning note: drafted
- First Phase 7 coding slice implementation: accepted on backend/API sanity check
- Phase 7 second coding slice planning note: drafted
- Second Phase 7 coding slice implementation: accepted on backend/API sanity check
- Phase 7 third coding slice planning note: drafted
- Third Phase 7 coding slice implementation: accepted on frontend build/browser sanity check
- Phase 7 fourth coding slice planning note: drafted
- Fourth Phase 7 coding slice implementation: accepted on backend/API sanity check
- Phase 7 fifth coding slice planning note: drafted
- Fifth Phase 7 coding slice implementation: accepted on backend/API sanity check
- Phase 7 sixth coding slice planning note: drafted
- Sixth Phase 7 coding slice implementation: accepted on frontend build/browser sanity check
- Phase 7 seventh coding slice planning note: drafted
- Seventh Phase 7 coding slice implementation: accepted on backend/API sanity check
- Phase 7 completion review note: drafted
- Phase 7 completion decision: accepted with deferred enhancements
- Phase 8 first coding slice planning note: drafted
- First Phase 8 coding slice implementation: accepted on backend/runtime smoke sanity check
- Phase 8 second coding slice planning note: drafted
- Second Phase 8 coding slice implementation: accepted on frontend build/browser runtime smoke sanity check
- Phase 8 third coding slice planning note: drafted
- Third Phase 8 coding slice implementation: accepted on backend/runtime smoke sanity check
- Phase 8 fourth coding slice planning note: drafted
- Fourth Phase 8 coding slice implementation: accepted on frontend build/browser runtime smoke sanity check
- Phase 8 completion review note: drafted
- Phase 8 completion decision: accepted with deferred enhancements
- Phase 9 first hardening slice planning note: drafted
- Phase 9 pilot hardening audit checklist: drafted
- Phase 9 second hardening slice planning note: drafted
- Second Phase 9 hardening slice implementation: accepted on runtime smoke sanity check
- Phase 9 third hardening slice planning note: drafted
- Third Phase 9 hardening slice implementation: accepted on runtime smoke sanity check
- Phase 9 fourth hardening slice planning note: drafted
- Fourth Phase 9 hardening slice implementation: accepted on runtime smoke sanity check
- Phase 9 completion review note: drafted
- Phase 9 completion decision: accepted with deferred pilot follow-ups
- MVP pilot cut summary: drafted
- MVP pilot full smoke gate rerun planning note: drafted
- MVP pilot full smoke gate suite rerun: accepted on runtime smoke suite
- MVP pilot packaging runbook planning note: drafted
- MVP pilot local runbook: drafted
- MVP pilot smoke orchestration planning note: drafted
- MVP pilot smoke suite orchestration: accepted on runtime smoke suite
- MVP pilot import matrix smoke planning note: drafted
- MVP pilot contact and opportunity import matrix smoke: accepted on runtime smoke suite
- MVP pilot metadata rollback smoke planning note: drafted
- MVP pilot metadata rollback smoke: accepted on runtime smoke suite
- MVP pilot CI workflow planning note: drafted
- MVP pilot CI smoke workflow: accepted on build/runtime smoke suite
- MVP pilot production deployment planning note: drafted
- MVP pilot production deployment packaging baseline: accepted on production image build/deployment smoke sanity check
- MVP pilot explicit migration command: accepted on isolated production migration sanity check
- MVP pilot backup/restore drill: accepted on isolated PostgreSQL backup/restore drill
- MVP pilot rollback dry run: accepted on isolated production Compose rollback dry run
- MVP pilot staging/secrets baseline: accepted on env validation and staging compose config sanity check
- MVP pilot external staging handoff: accepted on local handoff package validation
- MVP pilot managed secrets planning: accepted on provider-neutral secret mapping validation
- MVP pilot production platform/IaC planning: accepted as provider-neutral planning baseline
- MVP pilot single-node host IaC baseline: accepted on local host preflight sanity check
- MVP pilot reverse proxy/TLS handoff: accepted on local route validation sanity check
- MVP pilot image registry promotion baseline: accepted on image reference validation sanity check
- MVP pilot CI release automation baseline: accepted on production image build and release manifest validation
- MVP pilot registry push automation baseline: accepted on registry push dry-run validation
- MVP pilot staging manifest consumption: accepted on rendered deploy env validation
- MVP pilot staging deploy automation: accepted on staging deploy dry-run validation
- MVP pilot staging deploy apply drill: accepted on isolated staging deploy apply drill
- MVP pilot staging post-deploy gates: accepted on post-deploy gate dry-run validation
- MVP pilot staging post-deploy apply drill: accepted on isolated post-deploy apply drill
- MVP pilot production readiness summary: accepted as staging-package-ready production-readiness summary
- MVP pilot external staging acceptance run: accepted on external staging acceptance dry-run
- MVP pilot secret provider adapter contract: accepted on env/dotenv render validation
- First Phase 2 coding slice implementation: accepted on narrow runtime sanity check
- Second Phase 2 coding slice implementation: accepted on narrow runtime sanity check
- Third Phase 2 coding slice implementation: accepted on narrow runtime sanity check
- Fourth Phase 2 coding slice implementation: accepted on narrow runtime sanity check
- Fifth Phase 2 coding slice implementation: accepted on narrow runtime sanity check
- Sixth Phase 2 coding slice implementation: accepted on narrow runtime sanity check
- Seventh Phase 2 coding slice implementation: accepted on narrow runtime sanity check
- Eighth Phase 2 coding slice implementation: accepted on narrow runtime sanity check
- Ninth Phase 2 coding slice implementation: accepted on narrow frontend build/dev sanity check
- Tenth Phase 2 coding slice implementation: accepted on narrow frontend build/API sanity check
- Eleventh Phase 2 coding slice implementation: accepted on narrow frontend build/API sanity check
- Twelfth Phase 2 coding slice implementation: accepted on narrow frontend build/API sanity check
- Broader Phase 2 API/build/dev verification pass: accepted on current running compose stack
- Phase 2 clean restart/browser-level verification pass: accepted
- First Phase 3 coding slice implementation: accepted on migration/runtime sanity check
- Second Phase 3 coding slice implementation: accepted on backend compile/runtime sanity check
- Third Phase 3 coding slice implementation: accepted on backend compile/runtime sanity check
- Fourth Phase 3 coding slice implementation: accepted on backend compile/restart sanity check
- Fifth Phase 3 coding slice implementation: accepted on backend API/runtime sanity check
- Sixth Phase 3 coding slice implementation: accepted on frontend build/API sanity check
- Seventh Phase 3 coding slice implementation: accepted on frontend build/browser sanity check
- Eighth Phase 3 coding slice implementation: accepted on frontend build/API/browser sanity check
- Ninth Phase 3 coding slice implementation: accepted on frontend build/API/browser sanity check
- Phase 3 legal final approval and Sales Rep outcome verification: accepted on browser sanity check
- First Phase 4 coding slice implementation: accepted on migration/runtime sanity check
- Twelfth Phase 4 coding slice implementation: accepted on backend/frontend/API sanity check
- Thirteenth Phase 4 coding slice implementation: accepted on backend compile/API sanity check
- Phase 4 completion decision: accepted with legacy stage persistence bridge deferred
- First Phase 5 coding slice implementation: accepted on backend/frontend/API sanity check
- Second Phase 5 coding slice implementation: accepted on backend/frontend/API sanity check
- Third Phase 5 coding slice implementation: accepted on backend/frontend/API sanity check
- Fourth Phase 5 coding slice implementation: accepted on backend/API sanity check
- Fifth Phase 5 coding slice implementation: accepted on backend/API sanity check
- Sixth Phase 5 coding slice implementation: accepted on frontend/API sanity check
- Phase 5 completion decision: accepted with deferred enhancements
- First Phase 6 coding slice implementation: accepted on backend/API sanity check
- Second Phase 6 coding slice implementation: accepted on backend/API sanity check
- Third Phase 6 coding slice implementation: accepted on backend/API sanity check
- Fourth Phase 6 coding slice implementation: accepted on backend/API sanity check
- Fifth Phase 6 coding slice implementation: accepted on frontend build/browser sanity check
- Sixth Phase 6 coding slice implementation: accepted on backend/API sanity check
- Seventh Phase 6 coding slice implementation: accepted on frontend build/browser sanity check
- Eighth Phase 6 coding slice implementation: accepted on backend/API sanity check
- Ninth Phase 6 coding slice implementation: accepted on frontend build/browser sanity check
- First Phase 1 coding slice implementation: started
- Second Phase 1 coding slice implementation: implemented without runtime verification
- Third Phase 1 coding slice implementation: implemented without runtime verification
- Codebase bootstrap: smoke runtime gate passed
- Implementation: started

## Current Phase

- Post-pilot secret provider adapter contract accepted

## What is already done

- created `codebase/backend` shell for Kotlin + Spring Boot;
- created `codebase/frontend` shell for React + Vite;
- added PostgreSQL baseline and Flyway bootstrap migration;
- added temporary demo login flow with seeded tenant/users/roles;
- added `/healthz`, `/readyz`, `/api/session/demo-users`, `/api/session/demo-login`, `/api/me`;
- added local `docker-compose.yml`;
- verified `docker compose config --quiet`.
- started the first Phase 1 coding slice on backend:
  - extracted `UserShellRepository`;
  - added `CurrentUserContext`, `TenantContext`, `SessionResolver`, `ShellModuleVisibilityPolicy`;
  - added `ApiExceptionHandler`;
  - refactored `SessionService` and `SessionController` toward a thinner shell/auth boundary.
- implemented `V2__phase1_identity_hardening.sql`:
  - added `app_users.status` constraint;
  - added `(tenant_id, status)` index;
  - added `finance_approver` and `legal_approver` roles;
  - added seeded approver users and role assignments.
- defined the third Phase 1 slice as `frontend shell/session refinement`.
- implemented the third Phase 1 frontend shell/session refinement slice:
  - extracted frontend session contracts into `types/session.ts`;
  - extracted API transport into `api/session.ts`;
  - extracted local session persistence into `lib/sessionStorage.ts`;
  - added `LoginScreen`, `WorkspaceShell`, `ModuleGrid` and `SessionBanner`;
  - slimmed `App.tsx` down to shell-state orchestration;
  - made stale local session handling explicit on frontend.
- completed a disposable smoke runtime pass on `2026-05-07`:
  - `docker compose` stack started successfully;
  - backend `/healthz` returned `200 ok`;
  - backend `/readyz` returned `200 ready` with PostgreSQL `ok`;
  - `GET /api/session/demo-users` returned seeded users including `user_daria` and `user_oleg`;
  - `POST /api/session/demo-login` returned a valid session payload for `anna@orion.local`;
  - `GET /api/me` returned expected shell context for `user_anna`;
  - `GET /api/me` returned expected approver shell context for `user_daria`;
  - frontend responded on `http://localhost:5173`.
- started the first `Phase 2` coding slice in code:
  - added `V3__phase2_crm_core.sql`;
  - added backend account DTO/repository/service/controller baseline;
  - exposed planned `GET /api/accounts` and `POST /api/accounts` boundary in code;
  - added minimal `422 validation_failed` support for account-path validation;
  - passed a narrow backend sanity check on `2026-05-07` after a clean restart:
    - `V1`, `V2`, `V3` migration chain started successfully;
    - `/healthz` returned `200 ok`;
    - `/readyz` returned `200 ready` with PostgreSQL `ok`;
    - `GET /api/accounts` returned an empty list for `user_anna`;
    - `POST /api/accounts` created `Titan Plastics`;
    - `GET /api/accounts` then returned the created account for `user_anna`;
    - `GET /api/accounts` for `user_daria` returned `403 forbidden`.
- defined the second `Phase 2` slice as `backend contact baseline`.
- started the second `Phase 2` coding slice in code:
  - added `phase2_second_coding_slice_planning_note.md`;
  - added backend contact DTO/repository/service/controller baseline;
  - reused account visibility lookup to validate linked account scope for contact create;
  - passed a narrow backend sanity check on `2026-05-07`:
    - `GET /api/contacts` returned an empty list for `user_anna`;
    - `POST /api/contacts` created `Elena Sidorova` under `Titan Plastics`;
    - `GET /api/contacts?accountId=acc_9b74122f5fae` returned contact records under `Titan Plastics`;
    - `GET /api/contacts` for `user_daria` returned `403 forbidden`.
- defined the third `Phase 2` slice as `backend opportunity baseline`.
- started the third `Phase 2` coding slice in code:
  - added `phase2_third_coding_slice_planning_note.md`;
  - added backend opportunity DTO/repository/service/controller baseline;
  - added narrow stage lookup and opportunity detail/list/create path;
  - reused account/contact visibility lookups to validate linked scope on opportunity create;
  - passed a narrow backend sanity check on `2026-05-07`:
    - invalid `stageKey=qualificatio` returned `422 validation_failed`;
    - valid opportunity create returned `201` with `opp_d4834a09a090`;
    - `GET /api/opportunities/opp_d4834a09a090` returned expected account/contact/owner/stage payload;
    - `GET /api/opportunities` for `user_daria` returned `403 forbidden`.
- defined the fourth `Phase 2` slice as `backend activity baseline`.
- started the fourth `Phase 2` coding slice in code:
  - added `phase2_fourth_coding_slice_planning_note.md`;
  - added backend activity DTO/repository/service/controller baseline;
  - reused opportunity visibility lookup to validate activity list/create scope;
  - passed a narrow backend sanity check on `2026-05-07`:
    - activity list/create outputs matched the expected baseline for `user_anna`;
    - created activity returned `201`;
    - follow-up list returned an `open` activity;
    - activity browse for `user_daria` returned `403 forbidden`.
- defined the fifth `Phase 2` slice as `backend opportunity move-stage command`.
- implemented the fifth `Phase 2` coding slice in code:
  - added `phase2_fifth_coding_slice_planning_note.md`;
  - added `StageTransitionPolicy.kt`;
  - added `POST /api/opportunities/{id}/move-stage`;
  - added repository update path for stage/global-status mutation;
  - tightened opportunity create so `pending_approval` cannot be used before approval workflow exists;
  - passed a narrow backend sanity check on `2026-05-08` after restarting the backend service to pick up the mounted source:
    - `/healthz` returned `200 ok`;
    - `/readyz` returned `200 ready` with PostgreSQL `ok`;
    - setup account/contact/opportunity records were created for `user_anna`;
    - `POST /api/opportunities/{id}/move-stage` moved `qualification` to `negotiation` and returned `200`;
    - follow-up detail read returned `stageKey = negotiation`;
    - database state showed `stage_key = negotiation`, `global_status = active`, and `updated_by_user_id = user_anna`;
    - same-stage, approval-gated target, invalid-stage and closed-opportunity transitions returned `422 validation_failed`;
    - move-stage for `user_daria` returned `403 forbidden`.
- defined the sixth `Phase 2` slice as `backend opportunity patch/update baseline`.
- implemented the sixth `Phase 2` coding slice in code:
  - added `phase2_sixth_coding_slice_planning_note.md`;
  - added `PATCH /api/opportunities/{id}`;
  - added DTO/service/repository update path for `title`, `expectedAmount`, and `closeDate`;
  - kept stage mutation and owner reassignment outside `PATCH`;
  - passed `gradle compileKotlin --no-daemon` inside the backend container;
  - passed a narrow backend sanity check on `2026-05-08` after restarting the backend service:
    - `/healthz` returned `200 ok`;
    - `/readyz` returned `200 ready` with PostgreSQL `ok`;
    - setup account/contact/opportunity records were created for `user_anna`;
    - `PATCH /api/opportunities/{id}` updated `title`, `expectedAmount`, and `closeDate` and returned `200`;
    - follow-up detail read returned the patched values while keeping `stageKey = qualification`;
    - database state showed patched mutable fields and `updated_by_user_id = user_anna`;
    - blank title and empty patch returned `422 validation_failed`;
    - `stageKey` and `ownerId` in patch body returned `422 validation_failed`;
    - patch for `user_daria` returned `403 forbidden`.
- defined the seventh `Phase 2` slice as `backend opportunity reassign-owner command`.
- implemented the seventh `Phase 2` coding slice in code:
  - added `phase2_seventh_coding_slice_planning_note.md`;
  - added `POST /api/opportunities/{id}/reassign-owner`;
  - added DTO/service/repository update path for `owner_user_id`;
  - kept owner mutation outside `PATCH`;
  - kept manager team scope expansion outside this pass;
  - passed `gradle compileKotlin --no-daemon` inside the backend container;
  - passed a narrow backend sanity check on `2026-05-08` after restarting the backend service:
    - `/healthz` returned `200 ok`;
    - `/readyz` returned `200 ready` with PostgreSQL `ok`;
    - setup account/contact/opportunity records were created for `user_anna`;
    - Sales Rep and approver reassign attempts returned `403 forbidden`;
    - invalid target owner and same-owner reassignment returned `422 validation_failed`;
    - RevOps reassigned an opportunity from `user_anna` to `user_michael` and received `200`;
    - follow-up detail read as `user_michael` returned owner `user_michael`;
    - previous owner `user_anna` no longer saw the reassigned opportunity under current owner-only scope;
    - database state showed `owner_user_id = user_michael` and `updated_by_user_id = user_irina`;
    - Sales Manager reassigned an opportunity already visible in current own scope and received `200`.
- defined the eighth `Phase 2` slice as `backend manager/team opportunity scope baseline`.
- implemented the eighth `Phase 2` coding slice in code:
  - added `phase2_eighth_coding_slice_planning_note.md`;
  - added `TeamScopePolicy.kt`;
  - introduced a seed baseline where `user_michael` sees own opportunities plus `user_anna` opportunities;
  - changed opportunity list/detail/action visibility from single-owner scope to policy-resolved owner scope;
  - applied the same opportunity visibility lookup to activity list/create;
  - kept account/contact team scope, schema changes, saved views, sharing model, dashboards and frontend CRM outside this pass;
  - passed `gradle compileKotlin --no-daemon` inside the backend container;
  - passed a narrow backend sanity check on `2026-05-08` after restarting the backend service:
    - `/healthz` returned `200 ok`;
    - `/readyz` returned `200 ready` with PostgreSQL `ok`;
    - setup opportunity records were created for `user_anna` and `user_michael`;
    - Sales Rep list returned only own opportunity records;
    - Sales Manager list returned own plus `user_anna` team opportunity records;
    - RevOps list returned tenant opportunity records;
    - approver opportunity browse returned `403 forbidden`;
    - Sales Manager could detail, patch, move-stage, create/list activity and reassign owner on a `user_anna` opportunity;
    - Sales Rep could not detail a `user_michael` opportunity;
    - database state showed manager-applied title/stage/owner updates with `updated_by_user_id = user_michael`.
- defined the ninth `Phase 2` slice as `frontend CRM read shell`.
- implemented the ninth `Phase 2` coding slice in code:
  - added `phase2_ninth_coding_slice_planning_note.md`;
  - added frontend CRM types in `frontend/src/types/crm.ts`;
  - added account and opportunity read API clients;
  - added read-only account list, opportunity list and opportunity detail components;
  - wired authenticated workspace to render CRM read shell as the primary logged-in surface;
  - kept create forms, patch controls, move-stage controls, reassign-owner controls, activity create UI and approvals outside this pass;
  - added `vite-env.d.ts` so Vite env typing is available to TypeScript;
  - passed `npm run build` inside the frontend container;
  - passed a narrow dev-server sanity check on `2026-05-08`:
    - frontend dev server returned `200` from `http://localhost:5173` inside the frontend container;
    - backend opportunity read API returned `200` for `user_michael` under manager/team scope;
    - running compose services remained up for backend, frontend and database.
- defined the tenth `Phase 2` slice as `frontend CRM create forms`.
- implemented the tenth `Phase 2` coding slice in code:
  - added `phase2_tenth_coding_slice_planning_note.md`;
  - added frontend create contracts for account, contact and opportunity;
  - added contact read/create API client;
  - added create API functions for account and opportunity;
  - added `CrmCreatePanel.tsx` with account, contact and opportunity create forms;
  - wired selected account/contact state into the CRM workspace;
  - refreshed account/contact/opportunity read lists after successful creates;
  - kept patch controls, move-stage controls, reassign-owner controls, activity create UI and approvals outside this pass;
  - passed `npm run build` inside the frontend container;
  - passed a narrow dev/API sanity check on `2026-05-08`:
    - frontend dev server returned `200` from `http://localhost:5173` inside the frontend container;
    - account create returned `201`;
    - contact create under the new account returned `201`;
    - opportunity create under the new account/contact returned `201`;
    - contact list for the new account returned the created contact;
    - opportunity detail returned the created account/contact/opportunity linkage.
- defined the eleventh `Phase 2` slice as `frontend opportunity action controls`.
- implemented the eleventh `Phase 2` coding slice in code:
  - added `phase2_eleventh_coding_slice_planning_note.md`;
  - added frontend contracts/API clients for opportunity patch, move-stage and reassign-owner commands;
  - added opportunity detail controls for editing title/expected amount/close date;
  - added opportunity detail controls for moving between baseline stages;
  - added opportunity detail control for reassigning owner by user id;
  - refreshed opportunity list/detail after successful action commands;
  - kept activity create UI, approvals, stage catalog UI and user picker/autocomplete outside this pass;
  - passed `npm run build` inside the frontend container;
  - passed a narrow dev/API sanity check on `2026-05-08`:
    - frontend dev server returned `200` from `http://localhost:5173` inside the frontend container;
    - opportunity patch returned `200`;
    - opportunity move-stage returned `200`;
    - opportunity reassign-owner returned `200`;
    - follow-up detail returned patched title/amount/date, moved stage and reassigned owner.
- defined the twelfth `Phase 2` slice as `frontend activity section on opportunity detail`.
- implemented the twelfth `Phase 2` coding slice in code:
  - added `phase2_twelfth_coding_slice_planning_note.md`;
  - added frontend activity list/create contracts;
  - added `frontend/src/api/activities.ts`;
  - added activity list rendering inside opportunity detail;
  - added minimal activity create form with `type`, `title`, and `dueDate`;
  - refreshed activity list after successful activity create;
  - kept activity edit/complete/delete, approvals and audit timeline expansion outside this pass;
  - passed `npm run build` inside the frontend container;
  - passed a narrow dev/API sanity check on `2026-05-08`:
    - frontend dev server returned `200` from `http://localhost:5173` inside the frontend container;
    - initial activity list returned `200` with an empty list for a new opportunity;
    - activity create returned `201`;
    - follow-up activity list returned the created open activity.
- completed a broader `Phase 2` API/build/dev verification pass on `2026-05-08` against the current running compose stack:
  - `docker compose exec -T backend gradle compileKotlin --no-daemon` passed;
  - `docker compose exec -T frontend npm run build` passed;
  - frontend dev server returned `200` from `http://localhost:5173` inside the frontend container;
  - backend `/healthz` returned `200`;
  - backend `/readyz` returned `200`;
  - API walkthrough run `1778193830583` created account `acc_64b3630bbd3e`, contact `con_99cc72a79318`, opportunity `opp_51e22e2980e1`, and activity `act_35ac48e2563e`;
  - Sales Rep created account/contact/opportunity, patched mutable opportunity fields, and moved stage from `qualification` to `negotiation`;
  - Sales Manager saw the rep-owned opportunity through team scope, patched it, created/listed an activity, and reassigned ownership to self;
  - previous Sales Rep owner no longer had direct detail visibility after reassignment (`422 validation_failed` from the visible-scope lookup);
  - RevOps could read the reassigned opportunity through tenant scope;
  - Approver opportunity browse remained denied with `403 forbidden`.
- completed a clean restart and browser-level `Phase 2` verification pass on `2026-05-08`:
  - ran `docker compose down` followed by `docker compose up --build -d` without deleting named volumes;
  - PostgreSQL came back healthy;
  - backend started successfully after restart;
  - Flyway validated `3` migrations, reported current schema version `3`, and found no pending migration;
  - frontend dev server started successfully after restart;
  - backend `/healthz` returned `200` with `status = ok`;
  - backend `/readyz` returned `200` with PostgreSQL `ok`;
  - `docker compose exec -T backend gradle compileKotlin --no-daemon` passed after restart;
  - `docker compose exec -T frontend npm run build` passed after restart;
  - headless Chrome browser walkthrough run `1778194361308` logged in as `anna@orion.local`;
  - the browser created `Browser Account 1778194361308`, `Browser Contact 1778194361308`, `Browser Opportunity 1778194361308`, and `Browser Activity 1778194361308` through the UI;
  - the browser patched the opportunity to `Browser Patched 1778194361308`;
  - the browser moved the opportunity stage to `negotiation`;
  - browser refresh restored the authenticated workspace from `localStorage` with `salesops-demo-user-id = user_anna`;
  - `Switch User` returned to the login screen and cleared `salesops-demo-user-id`;
  - screenshot artifact was captured at `/tmp/salesops-crm-phase2-browser-1778194361308.png`;
  - no browser runtime exceptions or non-aborted network loading failures were observed by the CDP walkthrough.
- defined the first `Phase 3` slice as `backend approval database foundation`.
- implemented the first `Phase 3` coding slice in code:
  - added `phase3_first_coding_slice_planning_note.md`;
  - added `V4__phase3_approval_core.sql`;
  - introduced separate `approval_requests`, `approval_steps`, and `approval_decision_history` tables;
  - kept approval request, step and append-only decision history storage separate from `opportunities`;
  - added request/step/event lifecycle check constraints;
  - added tenant/status/approver/request indexes;
  - added a partial uniqueness guard against conflicting active approval requests per opportunity/request type/policy scope;
  - kept submit endpoints, approver inbox, decision commands, policy resolver, opportunity bridge and frontend approval UI outside this pass;
  - passed a migration/runtime sanity check on `2026-05-08` after restarting the backend service:
    - Flyway validated `4` migrations;
    - Flyway migrated schema `public` to version `4 - phase3 approval core`;
    - `flyway_schema_history` showed versions `1`, `2`, `3`, and `4` successful;
    - backend `/healthz` returned `200` with `status = ok`;
    - backend `/readyz` returned `200` with PostgreSQL `ok`;
    - `docker compose exec -T backend gradle compileKotlin --no-daemon` passed.
- defined the second `Phase 3` slice as `backend approval DTOs and repository baseline`.
- implemented the second `Phase 3` coding slice in code:
  - added `phase3_second_coding_slice_planning_note.md`;
  - added `approval/ApprovalDtos.kt`;
  - added `approval/ApprovalRepository.kt`;
  - introduced DTO shapes for future submit, inbox, detail and decision endpoints;
  - introduced repository commands and records for approval request, approval step, decision history and inbox access;
  - added repository methods for request create, step create, history append, request/step status updates, request detail loading, step/history loading and active approver inbox listing;
  - kept controller endpoints, lifecycle service, policy resolver, visibility policy, opportunity bridge and frontend approval UI outside this pass;
  - passed `docker compose exec -T backend gradle compileKotlin --no-daemon`;
  - existing backend `/healthz` returned `200` with `status = ok`;
  - existing backend `/readyz` returned `200` with PostgreSQL `ok`.
- defined the third `Phase 3` slice as `backend approval policy/state/visibility baseline`.
- implemented the third `Phase 3` coding slice in code:
  - added `phase3_third_coding_slice_planning_note.md`;
  - added `approval/ApprovalPolicyResolver.kt`;
  - added `approval/ApprovalStatePolicy.kt`;
  - added `approval/ApprovalVisibilityPolicy.kt`;
  - introduced limited MVP policy resolution for `stage_progression` approvals;
  - introduced standard vs large-deal policy distinction at `50000.00`;
  - introduced sequential role-step construction for `finance_approver` and `legal_approver`;
  - centralized approval request and step transition validation;
  - centralized submit, inbox, request view and active-step action visibility decisions;
  - kept approval service orchestration, controller endpoints, opportunity lifecycle bridge and frontend approval UI outside this pass;
  - passed `docker compose exec -T backend gradle compileKotlin --no-daemon`;
  - existing backend `/healthz` returned `200` with `status = ok`;
  - existing backend `/readyz` returned `200` with PostgreSQL `ok`.
- defined the fourth `Phase 3` slice as `backend approval service and opportunity bridge baseline`.
- implemented the fourth `Phase 3` coding slice in code:
  - added `phase3_fourth_coding_slice_planning_note.md`;
  - added `approval/ApprovalService.kt`;
  - added `crm/opportunity/OpportunityApprovalBridge.kt`;
  - added opportunity approval context lookup and approval state update paths to `OpportunityRepository.kt`;
  - wired submit approval orchestration, approver inbox assembly, approval detail assembly and approve/reject/send-back orchestration;
  - kept the approval service dependent on opportunity through `OpportunityApprovalBridge`;
  - kept controller endpoints, frontend approval UI, notifications and metadata-driven policy configuration outside this pass;
  - initially hit a runtime binding issue where PostgreSQL could not infer `java.time.Instant` for `timestamptz` parameters;
  - fixed repository timestamp binding by converting `Instant` to UTC `OffsetDateTime` at JDBC boundaries;
  - passed `docker compose exec -T backend gradle compileKotlin --no-daemon`;
  - backend restarted successfully with Flyway schema version `4` up to date;
  - existing backend `/readyz` returned `200` with PostgreSQL `ok`.
- defined the fifth `Phase 3` slice as `backend approval controller endpoints`.
- implemented the fifth `Phase 3` coding slice in code:
  - added `phase3_fifth_coding_slice_planning_note.md`;
  - added `api/ApprovalController.kt`;
  - exposed `POST /api/opportunities/{opportunityId}/submit-approval`;
  - exposed `GET /api/approvals/inbox`;
  - exposed `GET /api/approvals/{approvalRequestId}`;
  - exposed `POST /api/approvals/{approvalRequestId}/approve`;
  - exposed `POST /api/approvals/{approvalRequestId}/reject`;
  - exposed `POST /api/approvals/{approvalRequestId}/send-back`;
  - kept frontend approval UI, opportunity detail approval strip, notifications and browser-level approval workflow outside this pass;
  - passed `docker compose exec -T backend gradle compileKotlin --no-daemon`;
  - backend restarted successfully with the controller in the Spring context;
  - passed a backend API sanity check on `2026-05-08`:
    - `/healthz` returned `200`;
    - `/readyz` returned `200` with PostgreSQL `ok`;
    - setup account `acc_3eeccae88423`, contact `con_002a11f6ffb8`, and opportunity `opp_10af710a73f5` were created for `user_anna`;
    - `POST /api/opportunities/opp_10af710a73f5/submit-approval` created approval request `apr_8ad05df6cc9c`;
    - approval detail showed a two-step large-deal policy with active `finance_approver` step and inactive `legal_approver` step;
    - `user_daria` finance approver inbox included `apr_8ad05df6cc9c`;
    - `user_michael` manager approval decision attempt returned `403 forbidden`;
    - `user_daria` approved the first step and the request stayed `pending_step`;
    - `user_oleg` legal approver inbox then included `apr_8ad05df6cc9c`;
    - `user_oleg` approved the second step and the request became `approved`;
    - final approval detail showed all steps approved and append-only history count `6`;
    - linked opportunity detail returned `approvalState = approved`.
- defined the sixth `Phase 3` slice as `frontend approval API and type contracts`.
- implemented the sixth `Phase 3` coding slice in code:
  - added `phase3_sixth_coding_slice_planning_note.md`;
  - added `frontend/src/types/approvals.ts`;
  - added `frontend/src/api/approvals.ts`;
  - added frontend contracts for submit, inbox, detail, approve, reject and send-back approval flows;
  - added frontend API helpers for all backend approval endpoints;
  - kept submit panel, approver inbox UI, approval detail UI, decision action UI and approval timeline UI outside this pass;
  - passed `docker compose exec -T frontend npm run build`;
  - frontend dev server returned `200`;
  - backend `GET /api/approvals/inbox` returned `200` for `user_daria`.
- defined the seventh `Phase 3` slice as `frontend opportunity approval submit strip`.
- implemented the seventh `Phase 3` coding slice in code:
  - added `phase3_seventh_coding_slice_planning_note.md`;
  - wired `submitApproval` into `CrmReadWorkspace.tsx`;
  - added a minimal approval action group to `OpportunityDetail.tsx`;
  - added business justification input and submit button;
  - disabled submit when the selected opportunity approval state is not `none`;
  - refreshed selected opportunity detail/list after successful submit;
  - kept approver inbox UI, approval request detail UI, decision action UI and approval timeline UI outside this pass;
  - passed `docker compose exec -T frontend npm run build`;
  - frontend dev server returned `200`;
  - backend `/readyz` returned `200` with PostgreSQL `ok`;
  - passed headless Chrome UI smoke on `2026-05-08`:
    - logged in as `anna@orion.local`;
    - created account `Approval UI Account 1778195709894`;
    - created contact `Approval UI Contact 1778195709894`;
    - created opportunity `Approval UI Opportunity 1778195709894`;
    - submitted approval from the opportunity detail approval strip with justification `Approval UI justification 1778195709894`;
    - page showed `Approval submitted` and pending approval state;
    - submit button became disabled after the opportunity entered pending approval;
    - no browser runtime exceptions or non-aborted network failures were observed by the CDP walkthrough.
- defined the eighth `Phase 3` slice as `frontend approver inbox UI`.
- implemented the eighth `Phase 3` coding slice in code:
  - added `phase3_eighth_coding_slice_planning_note.md`;
  - added `frontend/src/features/approvals/ApproverInbox.tsx`;
  - wired `WorkspaceShell.tsx` so finance approvers, legal approvers and RevOps can see the approval inbox;
  - restricted CRM workspace rendering to Sales Rep, Sales Manager and RevOps roles so approver-only users do not hit CRM `403` in the UI;
  - added focused inbox styling in `frontend/src/styles.css`;
  - kept approval request detail UI, decision action UI and approval timeline UI outside this pass;
  - passed `docker compose exec -T frontend npm run build`;
  - frontend dev server returned `200`;
  - backend `GET /api/approvals/inbox` returned `200` for `user_daria` with active request `apr_73c5cdf7279e`;
  - passed headless Chrome UI smoke on `2026-05-08`:
    - logged in as `daria@orion.local`;
    - page rendered `Approval Inbox`;
    - page showed `Approval UI Opportunity 1778195709894`;
    - CRM workspace controls were not rendered for the approver-only user;
    - no UI error box was rendered.
- defined the ninth `Phase 3` slice as `frontend approval detail and decision action UI`.
- implemented the ninth `Phase 3` coding slice in code:
  - added `phase3_ninth_coding_slice_planning_note.md`;
  - extended `ApproverInbox.tsx` with selectable approval rows and detail loading;
  - rendered request status, business justification, active step, ordered steps and recent history;
  - wired approve, reject and send-back actions with a shared decision comment;
  - refreshed approval detail and inbox after a decision;
  - kept dedicated approval routes, notifications, workflow builder and full multi-role browser walkthrough outside this pass;
  - passed `docker compose exec -T frontend npm run build`;
  - created fresh approval request `apr_d443b54205d1` for `Decision UI Opportunity 1778196767279`;
  - passed headless Chrome UI smoke on `2026-05-08`:
    - logged in as `daria@orion.local`;
    - selected the target approval request from the inbox;
    - detail panel rendered business justification `Decision UI justification 1778196767279`;
    - submitted approve with comment `Finance approval from UI smoke`;
    - finance step became `approved`;
    - legal step became `active`;
    - finance inbox no longer included the target request;
    - approve button became disabled after the request moved to the legal step;
    - no UI error box was rendered;
  - backend legal inbox for `user_oleg` returned `apr_d443b54205d1` with active `legal_approver` step.
- completed the tenth `Phase 3` verification slice:
  - added `phase3_tenth_verification_slice_planning_note.md`;
  - used the same browser session to log in as `oleg@orion.local`;
  - selected `Decision UI Opportunity 1778196767279` from the legal approval inbox;
  - submitted approve from the approval detail panel;
  - browser summary showed both `finance_approver` and `legal_approver` steps as `approved`;
  - legal inbox no longer included the completed request;
  - switched to `anna@orion.local`;
  - selected `Decision UI Opportunity 1778196767279` from the Sales Rep CRM opportunity list;
  - Sales Rep opportunity detail showed stage `qualification` and approval state `approved`;
  - no UI error box was rendered for Legal Approver or Sales Rep.
- defined the first `Phase 4` slice as `backend metadata schema foundation`.
- implemented the first `Phase 4` coding slice in code:
  - added `phase4_file_level_plan.md`;
  - added `phase4_first_coding_slice_planning_note.md`;
  - added `V5__phase4_metadata_core.sql`;
  - introduced `metadata_config_versions`;
  - introduced `metadata_field_definitions`;
  - introduced `metadata_stage_definitions`;
  - introduced `metadata_stage_required_fields`;
  - introduced `metadata_custom_field_values`;
  - seeded `tenant_orion` published metadata config `mcv_orion_v1`;
  - seeded metadata stage definitions for `qualification`, `negotiation`, and `pending_approval`;
  - kept metadata admin API, draft/publish service, frontend admin UI and metadata-aware CRM runtime outside this pass;
  - restarted backend and Flyway applied version `5`;
  - backend `/readyz` returned `200` with PostgreSQL `ok`;
  - database query showed Flyway versions `1` through `5`;
  - database query showed `mcv_orion_v1` as published version `1`;
  - database query showed the three seeded stage definitions in sort order;
  - `docker compose exec -T backend gradle compileKotlin --no-daemon` passed.
- defined the second `Phase 4` slice as `backend metadata DTO/repository read baseline`.
- implemented the second `Phase 4` coding slice in code:
  - added `phase4_second_coding_slice_planning_note.md`;
  - added `MetadataDtos.kt`;
  - added `MetadataRepository.kt`;
  - introduced DTO shapes for future published metadata response projection;
  - introduced repository records for config versions, field definitions, stage definitions, and required-field rules;
  - added published metadata snapshot read path for a tenant;
  - added focused read methods for published config, fields, stages, and required-field rules;
  - kept metadata admin API, draft/publish validation, frontend metadata admin UI and metadata-aware CRM runtime outside this pass;
  - passed `docker compose exec -T backend gradle compileKotlin --no-daemon`;
  - restarted backend successfully with Flyway schema version `5` up to date;
  - backend `/readyz` returned `200` with PostgreSQL `ok` from inside the compose network.
- defined the third `Phase 4` slice as `backend draft/publish validation service`.
- implemented the third `Phase 4` coding slice in code:
  - added `phase4_third_coding_slice_planning_note.md`;
  - extended metadata DTOs with validation and publish response shapes;
  - extended `MetadataRepository.kt` with config snapshot loading and publish status updates;
  - added `MetadataValidationPolicy.kt`;
  - added `MetadataService.kt`;
  - added RevOps-only boundary for draft validation and publish;
  - added validation for supported entities, supported field types, stable keys, labels, sort order, select options, stages, and required-field references;
  - added transactional publish behavior that archives the current published config and publishes the validated draft;
  - kept metadata admin endpoints, draft edit commands, frontend metadata admin UI and metadata-aware CRM runtime outside this pass;
  - passed `docker compose exec -T backend gradle compileKotlin --no-daemon`;
  - restarted backend successfully with Flyway schema version `5` up to date;
  - backend `/readyz` returned `200` with PostgreSQL `ok` from inside the compose network.
- defined the fourth `Phase 4` slice as `backend metadata admin endpoints`.
- implemented the fourth `Phase 4` coding slice in code:
  - added `phase4_fourth_coding_slice_planning_note.md`;
  - added `MetadataController.kt`;
  - extended metadata DTOs with create-draft request shape;
  - extended `MetadataRepository.kt` with draft lookup, next version number, config creation, and clone insert helpers for fields, stages, and required-field rules;
  - extended `MetadataService.kt` with create draft from published config, current draft read, and draft detail read;
  - exposed published metadata read, draft create, current draft read, draft detail read, draft validation, and draft publish endpoints;
  - kept field/stage edit endpoints, frontend metadata admin UI, metadata-aware CRM forms, and runtime replacement of `opportunity_stages` outside this pass;
  - passed `docker compose exec -T backend gradle compileKotlin --no-daemon`;
  - restarted backend successfully with Flyway schema version `5` up to date;
  - backend `/readyz` returned `200` with PostgreSQL `ok` from inside the compose network;
  - API smoke verified:
    - `GET /api/metadata/published` returned the active published config;
    - Sales Rep draft creation returned `403 forbidden`;
    - RevOps Admin created draft config `mcv_058a679a619e`;
    - `GET /api/metadata/drafts/current` returned the draft;
    - `POST /api/metadata/drafts/mcv_058a679a619e/validate` returned `valid = true`;
    - duplicate draft creation returned `422 validation_failed`;
    - `POST /api/metadata/drafts/mcv_058a679a619e/publish` published the draft;
    - database query showed `mcv_orion_v1` archived and `mcv_058a679a619e` published.
- defined the fifth `Phase 4` slice as `frontend metadata admin read/create draft UI`.
- implemented the fifth `Phase 4` coding slice in code:
  - added `phase4_fifth_coding_slice_planning_note.md`;
  - added `frontend/src/types/metadata.ts`;
  - added `frontend/src/api/metadata.ts`;
  - added `MetadataAdminWorkspace.tsx`;
  - connected Metadata Admin workspace to `WorkspaceShell.tsx` for RevOps Admin only;
  - added metadata workspace CSS in `styles.css`;
  - implemented published metadata read, current draft read, create draft, validate draft and publish draft frontend calls;
  - rendered config version, draft state, stage count, field count, required-rule count, stages and validation issues;
  - kept field/stage edit UI, metadata-aware CRM forms and runtime replacement of `opportunity_stages` outside this pass;
  - passed `docker compose exec -T frontend npm run build`;
  - passed headless Chrome UI smoke on `2026-05-08`:
    - logged in as `irina@orion.local`;
    - Metadata Admin workspace rendered;
    - published metadata and stage list rendered;
    - created draft config `mcv_3b09748bf970`;
    - validated the draft successfully;
    - validation warning `no_active_custom_fields` rendered;
    - no UI error box was rendered.
- defined the sixth `Phase 4` slice as `runtime published metadata read path`.
- implemented the sixth `Phase 4` coding slice in code:
  - added `phase4_sixth_coding_slice_planning_note.md`;
  - added `MetadataRuntimeService.kt`;
  - wired opportunity create and move-stage stage gates through published metadata runtime reads;
  - kept legacy `opportunity_stages` as the persistence bridge for opportunity `stage_id`;
  - kept metadata-aware CRM forms/detail display, required-field validation, and replacement of `opportunity_stages` query joins outside this pass;
  - passed `docker compose exec -T backend gradle compileKotlin --no-daemon`;
  - restarted backend successfully with Flyway schema version `5` up to date;
  - backend `/readyz` returned `200` with PostgreSQL `ok` from inside the compose network;
  - runtime/API smoke verified:
    - metadata config versions are `mcv_058a679a619e` published and `mcv_3b09748bf970` draft;
    - `GET /api/metadata/published` returned published version `2`;
    - created opportunity `opp_02e913660754` with published stage `qualification`;
    - invalid stage `draft_only_stage` returned `422 validation_failed` with `Published metadata stage key does not exist`;
    - moved `opp_02e913660754` to published stage `negotiation`;
    - opportunity readback showed `stageKey = negotiation`.
- defined the seventh `Phase 4` slice as `metadata-aware CRM forms/detail display`.
- implemented the seventh `Phase 4` coding slice in code:
  - added `phase4_seventh_coding_slice_planning_note.md`;
  - updated `CrmReadWorkspace.tsx` to load published metadata alongside CRM lists;
  - updated `CrmCreatePanel.tsx` to render opportunity stage options from published metadata;
  - updated `OpportunityList.tsx` to display published stage labels;
  - updated `OpportunityDetail.tsx` to display published stage labels and use metadata stage options for move-stage;
  - kept metadata-aware required-field validation, custom field rendering/editing, and backend query replacement outside this pass;
  - passed `docker compose exec -T frontend npm run build`;
  - passed headless Chrome UI smoke on `2026-05-08`:
    - logged in as `anna@orion.local`;
    - CRM workspace rendered published stage labels `Qualification`, `Negotiation`, and `Pending Approval`;
    - opportunity create form rendered a metadata-driven stage selector;
    - created opportunity `opp_51ae1671bb96` with title `Metadata-Aware UI Opportunity 1778233259785` through stage `negotiation`;
    - opportunity list/detail showed metadata stage label `Negotiation`;
    - no UI error box was rendered.
- defined the eighth `Phase 4` slice as `metadata-aware stage validation`.
- implemented the eighth `Phase 4` coding slice in code:
  - added `phase4_eighth_coding_slice_planning_note.md`;
  - added `MetadataStandardFieldKeys.kt` as the shared standard opportunity field-key source;
  - updated `MetadataValidationPolicy.kt` to reuse the shared standard opportunity field keys;
  - updated `OpportunityService.kt` to validate published metadata required-field rules on opportunity create and move-stage;
  - kept metadata admin edit endpoints, custom field value forms/APIs, structured error details and backend query replacement outside this pass;
  - passed `docker compose exec -T backend gradle compileKotlin --no-daemon`;
  - restarted backend successfully and verified `/readyz`;
  - passed runtime/API smoke on `2026-05-08`:
    - confirmed published metadata version `2` has no required rules after smoke cleanup;
    - created `opp_5b7d2e0fff26` and moved it from `qualification` to `negotiation` with no required rules;
    - temporarily inserted required rule `negotiation -> expected_amount`;
    - create directly into `negotiation` without `expectedAmount` returned `422 validation_failed`;
    - moving `opp_ff82bd049381` into `negotiation` without `expectedAmount` returned `422 validation_failed`;
    - create directly into `negotiation` with `expectedAmount` succeeded as `opp_26de8eb1abe8`;
    - deleted the temporary required rule and confirmed `GET /api/metadata/published` returned an empty `requiredFields` list.
- defined the ninth `Phase 4` slice as `metadata admin draft edit endpoints`.
- implemented the ninth `Phase 4` coding slice in code:
  - added `phase4_ninth_coding_slice_planning_note.md`;
  - extended `MetadataDtos.kt` with draft edit request DTOs for fields, stages and required-field rules;
  - extended `MetadataController.kt` with draft-only field/stage/required-field mutation endpoints;
  - extended `MetadataService.kt` with RevOps-only draft mutation orchestration, input normalization and updated snapshot readback;
  - extended `MetadataRepository.kt` with update/delete mutations for draft fields, stages and required-field rules;
  - kept frontend metadata edit UI, custom field value capture/rendering, publishing-smoke config changes and backend query replacement outside this pass;
  - passed `docker compose exec -T backend gradle compileKotlin --no-daemon`;
  - restarted backend successfully and verified `/readyz`;
  - passed runtime/API smoke on `2026-05-08` against draft `mcv_3b09748bf970`:
    - created draft field `mfd_44f0f4c0cf13` as `phase4_smoke_score`;
    - updated that field to `phase4_smoke_amount`;
    - created draft stage `msd_edc5273b9384` as `phase4_smoke_stage`;
    - updated that stage to `phase4_smoke_stage_updated`;
    - created required-field rule `msrf_dbb157ea7cb5`;
    - validated the draft successfully with the temporary field/stage/rule present;
    - deleted the temporary required-field rule, stage and field;
    - validated the cleaned-up draft successfully, with only the expected `no_active_custom_fields` warning.
- defined the tenth `Phase 4` slice as `custom field value capture/rendering`.
- implemented the tenth `Phase 4` coding slice in code:
  - added `phase4_tenth_coding_slice_planning_note.md`;
  - updated `MetadataRuntimeService.kt` to expose active published field definitions in runtime snapshots;
  - extended opportunity DTOs with `customFields` on create/update/detail;
  - extended `OpportunityRepository.kt` with typed custom field value read/upsert support for `metadata_custom_field_values`;
  - updated `OpportunityService.kt` to normalize custom values by published metadata type, save values on create/update, return values on detail, and enforce custom required-field rules on create/move-stage;
  - updated frontend CRM types and workspace wiring to pass published opportunity custom fields into create/detail UI;
  - updated `CrmCreatePanel.tsx` to render metadata-driven custom field inputs and send `customFields` on create;
  - updated `OpportunityDetail.tsx` to render custom field values and include custom field edits in Save Fields;
  - kept custom fields in list/filter views, frontend metadata edit UI, published-config rollback UI, and backend query replacement outside this pass;
  - published metadata config `mcv_3b09748bf970` as version `3` with active opportunity custom field `payment_risk_level` and required rule `negotiation -> payment_risk_level`;
  - passed `docker compose exec -T backend gradle compileKotlin --no-daemon`;
  - passed `docker compose exec -T frontend npm run build`;
  - restarted backend successfully and verified `/readyz`;
  - passed runtime/API smoke on `2026-05-08`:
    - create directly into `negotiation` without `payment_risk_level` returned `422 validation_failed`;
    - create directly into `negotiation` with `customFields.payment_risk_level = high` succeeded as `opp_3e02ffb264db`;
    - detail readback for `opp_3e02ffb264db` returned `customFields.payment_risk_level = high`;
    - created `opp_22b779528b6c` in `qualification` without the custom field;
    - move-stage to `negotiation` was blocked until `payment_risk_level` was patched to `low`;
    - move-stage to `negotiation` succeeded after the custom value patch;
  - passed headless Chrome UI smoke on `2026-05-08`:
    - logged in as `anna@orion.local`;
    - CRM workspace rendered `Payment Risk Level`;
    - create opportunity form selected `High` and created a negotiation opportunity;
    - detail/list view showed the created opportunity with no `.error-box`.
- defined the eleventh `Phase 4` slice as `frontend metadata admin field/stage edit UI`.
- implemented the eleventh `Phase 4` coding slice in code:
  - added `phase4_eleventh_coding_slice_planning_note.md`;
  - extended `frontend/src/types/metadata.ts` with draft edit request types;
  - extended `frontend/src/api/metadata.ts` with field, stage and required-rule mutation wrappers;
  - updated `MetadataAdminWorkspace.tsx` with draft field create/edit/delete controls;
  - updated `MetadataAdminWorkspace.tsx` with draft stage create/edit/delete controls;
  - updated `MetadataAdminWorkspace.tsx` with required-field rule create/delete controls;
  - updated `styles.css` for metadata editor forms, row actions and long-text inputs;
  - kept custom field list/filter support, metadata rollback UI, draft deletion and backend query replacement outside this pass;
  - passed `docker compose exec -T frontend npm run build`;
  - passed headless Chrome UI smoke on `2026-05-08`:
    - logged in as `irina@orion.local`;
    - created draft `mcv_1e56a0f18f59` from published metadata;
    - added field `ui_smoke_metric`, then updated its label to `UI Smoke Metric Updated`;
    - added stage `ui_smoke_stage`, then updated its label to `UI Smoke Stage Updated`;
    - added required-field rule `ui_smoke_stage -> ui_smoke_metric`;
    - validated the draft successfully;
    - deleted the temporary required rule, stage and field through the UI;
    - published the cleaned draft as metadata version `4`;
    - verified no `.error-box` was rendered.
- defined the twelfth `Phase 4` slice as `published metadata config rollback/new-draft management`.
- implemented the twelfth `Phase 4` coding slice in code:
  - added `phase4_twelfth_coding_slice_planning_note.md`;
  - extended metadata DTOs with config-version list response shape;
  - extended `MetadataRepository.kt` with config version listing, draft discard cleanup and archived-version publish path;
  - extended `MetadataService.kt` with RevOps-only version history, draft discard and rollback orchestration;
  - extended `MetadataController.kt` with config version list, draft discard and rollback endpoints;
  - extended frontend metadata API/types with version history, discard draft and rollback calls;
  - updated `MetadataAdminWorkspace.tsx` to render version history and expose discard/rollback actions;
  - kept config diff UI, partial rollback, custom field list/filter support and backend `opportunity_stages` query replacement outside this pass;
  - passed `docker compose exec -T backend gradle compileKotlin --no-daemon`;
  - passed `docker compose exec -T frontend npm run build`;
  - restarted backend successfully and verified `/readyz`;
  - passed reversible runtime/API smoke on `2026-05-08`:
    - initial versions were `4:published,3:archived,2:archived,1:archived`;
    - Sales Rep version-history access returned `403`;
    - RevOps Admin created draft version `5`;
    - rollback while draft was open returned `422`;
    - RevOps Admin discarded draft version `5`;
    - RevOps Admin created a new draft after discard and discarded it successfully;
    - rollback to archived version `3` returned `3:published`;
    - rollback back to original version `4` returned `4:published`;
    - final versions were restored to `4:published,3:archived,2:archived,1:archived`.
- defined the thirteenth `Phase 4` slice as `replace opportunity read joins with published metadata stage runtime`.
- implemented the thirteenth `Phase 4` coding slice in code:
  - added `phase4_thirteenth_coding_slice_planning_note.md`;
  - updated opportunity list/detail/approval read queries to return `o.stage_id` without joining `opportunity_stages`;
  - added a narrow legacy stage-id to stage-key bridge lookup for the current persistence boundary;
  - updated `OpportunityService.kt` to resolve opportunity response stage keys against the published metadata runtime snapshot;
  - updated stage filtering to validate requested stage keys against published metadata before querying and filter by bridge `stage_id`;
  - updated `OpportunityApprovalBridge.kt` to resolve approval submission snapshots through the published metadata runtime;
  - kept create/move-stage writes on legacy `stage_id` as the current persistence bridge and kept schema migration out of scope;
  - confirmed no `JOIN opportunity_stages` remains in opportunity list/detail/approval read SQL;
  - passed `docker compose exec -T backend gradle compileKotlin --no-daemon`;
  - restarted backend successfully and verified `/readyz`;
  - passed runtime/API smoke on `2026-05-08`:
    - created opportunity `opp_06c1f2d14a58` in published stage `qualification`;
    - `GET /api/opportunities?stage=qualification` returned the smoke opportunity;
    - detail read returned `stageKey = qualification`;
    - invalid unpublished stage filter returned `422`;
    - move-stage to `negotiation` was correctly blocked until published required field `payment_risk_level` was patched;
    - after patching `customFields.payment_risk_level = low`, move-stage to `negotiation` returned `updated = true`;
    - `GET /api/opportunities?stage=negotiation` returned the smoke opportunity;
    - detail read returned `stageKey = negotiation`.
- completed the Phase 4 exit decision:
  - added `phase4_completion_review_note.md`;
  - deferred the legacy `opportunities.stage_id` persistence bridge migration as schema/data cleanup;
  - entered Phase 5 planning with `phase5_first_coding_slice_planning_note.md`.
- implemented the first `Phase 5` coding slice in code:
  - added `V6__phase5_saved_views.sql` with tenant/user-scoped `saved_views` storage;
  - added backend saved-view DTO, repository, service and controller under `/api/opportunity-saved-views`;
  - validated saved-view stage/custom-field references against the current published metadata snapshot;
  - returned invalid saved views as marked invalid on list instead of silently applying stale metadata references;
  - kept saved views user-owned and opportunity-workspace-only in this baseline;
  - extended frontend opportunity API to send existing list filters;
  - added frontend saved-view API/types;
  - added opportunity workspace controls for stage/search filters, saved-view creation and saved-view apply;
  - passed `docker compose exec -T backend gradle compileKotlin --no-daemon`;
  - passed `docker compose exec -T frontend npm run build`;
  - restarted backend successfully and verified `/readyz` on port `8081`;
  - verified Flyway applied `V6` successfully;
  - passed API smoke on `2026-05-08`:
    - Sales Rep initial saved-view list returned empty;
    - Legal Approver saved-view list returned `403 forbidden`;
    - creating a saved view with unpublished stage `not_published` returned `422 validation_failed`;
    - Sales Rep created saved view `Phase 5 smoke qualification`;
    - follow-up saved-view list returned the created view with `valid = true`;
    - Manager saved-view list remained user-owned and returned no Sales Rep view.
- implemented the second `Phase 5` coding slice in code:
  - added `V7__phase5_saved_view_management.sql` with `visibility_scope` and shared-view index;
  - extended saved-view responses with owner identity, visibility scope and `canManage`;
  - added `PATCH /api/opportunity-saved-views/{savedViewId}`;
  - added `DELETE /api/opportunity-saved-views/{savedViewId}`;
  - kept update/delete restricted to the saved-view owner;
  - allowed `shared` saved views only for Sales Manager and RevOps Admin owners;
  - made shared views visible to CRM roles without changing opportunity record access scope;
  - extended frontend saved-view API/types for update/delete;
  - added saved-view visibility selection and per-view apply/update/delete controls to the opportunity list;
  - passed `docker compose exec -T backend gradle compileKotlin --no-daemon`;
  - passed `docker compose exec -T frontend npm run build`;
  - restarted backend successfully and verified `/readyz` on port `8081`;
  - verified Flyway applied `V7` successfully;
  - passed API smoke on `2026-05-08`:
    - RevOps Admin created shared view `Phase 5 shared smoke`;
    - Sales Rep and Sales Manager could list the shared view with `canManage = false`;
    - Sales Rep update/delete attempts against the RevOps shared view returned `403 forbidden`;
    - Sales Rep attempt to create a shared saved view returned `403 forbidden`;
    - RevOps Admin updated the shared view successfully;
    - Sales Rep opportunity list with the shared view filters still executed through Sales Rep scope;
    - RevOps Admin deleted the shared view successfully;
    - Sales Rep updated and deleted their own saved view successfully.
- implemented the third `Phase 5` coding slice in code:
  - extended `GET /api/opportunities` to accept custom field filters as `cf.{fieldKey}` query parameters;
  - validated custom field filter keys and values against the published metadata snapshot before query execution;
  - added access-aware custom field filtering through `metadata_custom_field_values` in the existing opportunity list SQL;
  - supported text/long text contains matching, single-select exact matching, numeric exact matching, date exact matching and boolean exact matching;
  - extended frontend opportunity API encoding for saved-view custom field filters;
  - added a single custom-field filter control to the opportunity saved-view panel;
  - kept custom-field list columns, arbitrary expressions and cross-entity filters out of scope;
  - passed `docker compose exec -T backend gradle compileKotlin --no-daemon`;
  - passed `docker compose exec -T frontend npm run build`;
  - restarted backend successfully and verified `/readyz` on port `8081`;
  - passed API smoke on `2026-05-08`:
    - created opportunity `opp_f37da195bc05` with `customFields.payment_risk_level = low`;
    - `GET /api/opportunities?cf.payment_risk_level=low&q=Phase%205%20Custom%20Field%20Filter%20Smoke` returned the smoke opportunity;
    - `GET /api/opportunities?cf.payment_risk_level=high&q=Phase%205%20Custom%20Field%20Filter%20Smoke` returned no records;
    - `GET /api/opportunities?cf.not_published=value` returned `422 validation_failed`;
    - `GET /api/opportunities?cf.payment_risk_level=medium` returned `422 validation_failed`;
    - Manager applying the same custom-field filter returned the team-visible smoke opportunity through existing manager scope;
    - saved views with `customFields.payment_risk_level = low` were created, listed as valid and then deleted after smoke verification.
- implemented the fourth `Phase 5` coding slice in code:
  - added `V8__phase5_manager_visibility.sql` with `manager_user_reports`;
  - seeded `tenant_orion` manager relationship `user_michael -> user_anna`;
  - added `ManagerVisibilityRepository.kt` to read active direct reports from persisted relationships;
  - updated `TeamScopePolicy.kt` so Sales Manager scope is self plus persisted active reports;
  - removed the hardcoded `user_michael -> user_anna` branch from Kotlin runtime;
  - kept RevOps Admin tenant-wide and Sales Rep own-record opportunity scope unchanged;
  - passed `docker compose exec -T backend gradle compileKotlin --no-daemon`;
  - recreated backend service so updated resources were picked up;
  - verified Flyway sees 8 migrations and schema is at version `8`;
  - verified `/readyz` on port `8081`;
  - passed API smoke on `2026-05-08`:
    - `manager_user_reports` contains `tenant_orion / user_michael / user_anna`;
    - Sales Rep could not see a manager-owned opportunity;
    - Sales Manager could see own manager-owned opportunity;
    - RevOps Admin could see the manager-owned opportunity;
    - Sales Manager still saw Anna-owned report opportunity through persisted relationship;
    - Sales Rep still saw Anna-owned own opportunity;
    - `rg` confirmed the old `seedTeamOwnerIds` branch is gone.
- implemented the fifth `Phase 5` coding slice in code:
  - changed account list and account visibility lookup to use the same owner-scope boundary as opportunities;
  - changed contact list and contact visibility lookup to use the same owner-scope boundary as opportunities;
  - injected `TeamScopePolicy` into account/contact services;
  - updated opportunity/contact create validation to allow accounts/contacts visible through the resolved manager team scope;
  - kept RevOps Admin tenant-wide behavior unchanged;
  - kept Sales Rep own-record behavior unchanged;
  - passed `docker compose exec -T backend gradle compileKotlin --no-daemon`;
  - restarted backend successfully and verified `/readyz` on port `8081`;
  - passed API smoke on `2026-05-08`:
    - Sales Manager could list Anna-owned account `Titan Plastics`;
    - Sales Manager could list contacts under Anna-owned `Titan Plastics`;
    - Sales Manager created opportunity `opp_14e3442476ce` against Anna-owned `Titan Plastics`;
    - Sales Manager created contact `con_c4edcb6082d5` under Anna-owned `Titan Plastics`;
    - Sales Rep could not list manager-owned account `Phase 5 Manager Scope Account 1778222630`;
    - Sales Rep could not see manager-owned opportunity `Phase 5 Manager Team Account Create Smoke 1778223000`;
    - RevOps Admin could list the manager-owned account.
- implemented the sixth `Phase 5` coding slice:
  - tightened frontend empty states for account lists, opportunity lists and saved views so filtered/team/tenant scopes are clearer;
  - kept backend code unchanged in this slice;
  - passed `docker compose exec -T frontend npm run build`;
  - verified backend `/readyz` on port `8081`;
  - passed direct-access and shared-view API regression on `2026-05-08`:
    - Sales Manager direct-opened report-owned opportunity `opp_f37da195bc05`;
    - Sales Rep direct-open attempt for manager-owned opportunity `opp_e5c493c6412b` returned `422 validation_failed`;
    - RevOps Admin direct-opened manager-owned opportunity `opp_e5c493c6412b`;
    - created RevOps-owned opportunity `opp_931ae04a427b`;
    - Sales Manager direct-open attempt for RevOps-owned opportunity `opp_931ae04a427b` returned `422 validation_failed`;
    - RevOps Admin direct-opened RevOps-owned opportunity `opp_931ae04a427b`;
    - RevOps Admin created shared saved view `Phase 5 revops shared no expansion`;
    - Sales Rep could list the shared saved view but applying its query returned no records;
    - Sales Manager applying the same shared-view query returned no records;
    - RevOps Admin applying the same query returned the RevOps-owned opportunity;
    - Sales Manager could list team account `Titan Plastics`;
    - Sales Manager could list activities for report-owned opportunity `opp_f37da195bc05`;
    - Sales Rep activity list against manager-owned opportunity `opp_e5c493c6412b` returned `422 validation_failed`;
    - RevOps Admin could list activities for manager-owned opportunity `opp_e5c493c6412b`.
- completed the Phase 5 exit decision:
  - added `phase5_completion_review_note.md`;
  - treated Phase 5 as functionally complete for the MVP path;
  - deferred richer saved-view sharing, custom field list columns, org chart UI, field-level security and legacy stage bridge cleanup;
  - entered Phase 6 planning with `phase6_first_coding_slice_planning_note.md`.
- implemented the first `Phase 6` coding slice in code:
  - added `V9__phase6_import_preview.sql` with `import_jobs` and `import_job_rows`;
  - added backend import DTOs, repository, service and controller;
  - added `POST /api/import-jobs/preview`;
  - restricted import preview creation to RevOps Admin;
  - supported account import preview only;
  - parsed CSV content from JSON request body;
  - supported explicit source-column to target-field mapping and narrow account field inference;
  - validated mapping targets against standard account fields and published account custom fields;
  - persisted job audit basics: tenant, initiator, entity type, file name, source columns, mapping, row counts and timestamps;
  - persisted row-level source data, preview data and validation errors;
  - kept preview read-only: no account records are created by preview;
  - passed `docker compose exec -T backend gradle compileKotlin --no-daemon`;
  - recreated backend service so Flyway picked up the new migration resource;
  - verified Flyway applied `V9` successfully and `/readyz` passed on port `8081`;
  - passed API smoke on `2026-05-08`:
    - RevOps Admin created preview job `ij_88125c4aaf49` for `phase6_accounts.csv`;
    - preview response returned source columns, one valid row and one invalid row;
    - invalid row reported `Account name is required`;
    - Sales Rep preview attempt returned `403 forbidden`;
    - Sales Manager preview attempt returned `403 forbidden`;
    - malformed CSV with unterminated quoted value returned `422 validation_failed`;
    - follow-up account list confirmed preview did not create `Phase 6 Import Preview Account 1778223900`;
    - database query confirmed persisted job totals and row-level validation errors.
- implemented the second `Phase 6` coding slice in code:
  - added `V10__phase6_import_execution.sql` with import job execution status/result columns;
  - extended import DTOs with job detail, row execution status, created record id and execution totals;
  - extended import repository with job lookup, row listing and row/job execution updates;
  - added account import execution in `ImportJobService`;
  - added `GET /api/import-jobs/{importJobId}` for job status/result reads;
  - added `POST /api/import-jobs/{importJobId}/execute` for RevOps Admin execution;
  - kept execution synchronous for the narrow baseline while persisting durable job/row outcomes;
  - created accounts for valid account preview rows as the executing RevOps Admin;
  - skipped invalid rows while preserving validation and execution errors;
  - rejected execution from Sales Rep and Sales Manager;
  - rejected repeat execution once a job leaves `previewed`;
  - passed `docker compose exec -T backend gradle compileKotlin --no-daemon`;
  - recreated backend service so Flyway picked up the new migration resource;
  - verified Flyway applied `V10` successfully and `/readyz` passed on port `8081`;
  - passed API smoke on `2026-05-08`:
    - RevOps Admin created preview job `ij_3cfe1c3b3c43` for `phase6_execute_probe.csv`;
    - job detail returned `previewed` status with both rows at `executionStatus = pending`;
    - Sales Rep execute attempt returned `403 forbidden`;
    - Sales Manager execute attempt returned `403 forbidden`;
    - RevOps Admin execute returned `executed` with `executedRows = 1` and `skippedRows = 1`;
    - valid row created account `acc_cae11e2961c7`;
    - invalid row was marked `skipped` with `Account name is required`;
    - repeated execute returned `422 validation_failed`;
    - account search found `Phase 6 Import Execute Probe 1778229000`.
- implemented the third `Phase 6` coding slice in code:
  - added `V11__phase6_export_jobs.sql` with durable export job storage;
  - added backend export DTOs, repository, service and controller under `bulkexport`;
  - added `POST /api/export-jobs` for RevOps Admin account export job creation;
  - added `GET /api/export-jobs/{exportJobId}` for export job status/result reads;
  - added account export repository read path that reuses the existing account visibility filter shape;
  - supported account `query`, `ownerId` and `limit` criteria;
  - generated synchronous CSV content for the narrow baseline;
  - persisted tenant, initiator, entity type, criteria, row count, CSV content and timestamps;
  - restricted export job creation and result reads to RevOps Admin;
  - passed `docker compose exec -T backend gradle compileKotlin --no-daemon`;
  - recreated backend service so Flyway picked up the new migration resource;
  - verified Flyway applied `V11` successfully and `/readyz` passed on port `8081`;
  - passed API smoke on `2026-05-08`:
    - created account `Phase 6 Export Probe 1778230100 Match`;
    - created non-matching account `Phase 6 Export Probe 1778230100 Other`;
    - Sales Rep export creation returned `403 forbidden`;
    - Sales Manager export creation returned `403 forbidden`;
    - RevOps Admin created export job `ej_523426c95b51`;
    - export job returned `completed` with `rowCount = 1`;
    - CSV contained the expected account header and matching account;
    - CSV did not contain the non-matching account;
    - RevOps Admin detail read returned the persisted CSV content;
    - Sales Manager detail read returned `403 forbidden`;
    - unsupported `contact` export returned `422 validation_failed`.
- implemented the fourth `Phase 6` coding slice in code:
  - added `V12__phase6_async_import_execution.sql` with queued/running import job statuses;
  - added import job `started_at`, `executed_by_user_id` and `failure_message` fields;
  - added `BulkJobConfig` with a small backend-local task executor;
  - added `ImportJobWorker` for single-process async account import execution;
  - changed `POST /api/import-jobs/{importJobId}/execute` to enqueue previewed jobs instead of processing rows inline;
  - schedules worker execution after the enqueue transaction commits;
  - worker moves jobs through `queued` -> `running` -> `executed` and preserves failed-job handling;
  - valid rows still create accounts as the executing RevOps Admin;
  - invalid rows are still marked `skipped` with row-level errors;
  - repeated execute/enqueue is rejected once the job leaves `previewed`;
  - passed `docker compose exec -T backend gradle compileKotlin --no-daemon`;
  - recreated backend service so Flyway picked up the new migration resource;
  - verified Flyway applied `V12` successfully and `/readyz` passed on port `8081`;
  - passed API smoke on `2026-05-09`:
    - RevOps Admin created preview job `ij_c42b9ecaa62d`;
    - Sales Rep execute attempt returned `403 forbidden`;
    - Sales Manager execute attempt returned `403 forbidden`;
    - RevOps Admin execute returned `queued` with pending rows;
    - job detail polling returned `executed` with `startedAt` and `executedAt`;
    - final totals were `executedRows = 1` and `skippedRows = 1`;
    - valid row created account `acc_09965fe715fd`;
    - invalid row was marked `skipped` with `Account name is required`;
    - repeated execute returned `422 validation_failed`;
    - account search found `Phase 6 Async Import Account 1778268400`.
- implemented the fifth `Phase 6` coding slice in code:
  - added frontend import/export job contracts in `types/bulkOperations.ts`;
  - added frontend import/export API wrappers in `api/bulkOperations.ts`;
  - added `BulkOperationsPanel` for RevOps Admin data operations;
  - integrated the panel into `CrmReadWorkspace` for `revops_admin` only;
  - added account import preview, execute/enqueue, refresh and polling controls;
  - added row-level import preview/execution outcome display;
  - added account export creation and CSV output display;
  - refreshed account lists after completed UI import execution;
  - kept Sales Rep and Sales Manager workspaces free of import/export controls;
  - passed `docker compose exec -T frontend npm run build`;
  - verified frontend dev server responds on port `5173`;
  - verified backend `/readyz` still passes on port `8081`;
  - passed browser smoke on `2026-05-09`:
    - RevOps Admin workspace showed `Data Operations`, `Preview` and `Export`;
    - Sales Rep workspace did not show `Data Operations` or `Preview`;
    - RevOps Admin created an import preview through the UI;
    - RevOps Admin executed the UI import and saw `executed`, `created` and row-level `Account name is required`;
    - RevOps Admin created export job `ej_7ec7e806b9a5`;
    - export CSV output contained `Phase 6 UI Import Account` and `https://ui-import.example`.
- implemented the sixth `Phase 6` coding slice in code:
  - added `V13__phase6_contact_import.sql` to allow `contact` import jobs;
  - added tenant-scoped account lookup for import resolution;
  - extended import preview mapping/validation for contacts;
  - supported contact target fields `fullName`, `email`, `phone`, `accountId` and `accountName`;
  - resolved contact account references by account id or exact account name during preview;
  - extended async import worker to create contacts through `ContactRepository`;
  - preserved existing account import behavior after the entity expansion;
  - passed `docker compose exec -T backend gradle compileKotlin --no-daemon`;
  - recreated backend service so Flyway picked up the new migration resource;
  - verified Flyway applied `V13` successfully and `/readyz` passed on port `8081`;
  - passed API smoke on `2026-05-09`:
    - RevOps Admin created account `acc_0a5ca4c1d093` for contact import resolution;
    - RevOps Admin created contact preview job `ij_4d6acab8f15a`;
    - preview resolved `Phase 6 Contact Import Account 1778278900` to account id `acc_0a5ca4c1d093`;
    - preview returned one valid contact row and one invalid row;
    - invalid row reported `Contact full name is required`;
    - Sales Rep contact preview returned `403 forbidden`;
    - Sales Manager contact preview returned `403 forbidden`;
    - Sales Rep contact execute returned `403 forbidden`;
    - Sales Manager contact execute returned `403 forbidden`;
    - RevOps Admin execute returned `queued`;
    - job detail polling returned `executed` with `executedRows = 1` and `skippedRows = 1`;
    - valid row created contact `con_9feeeb1a0c85`;
    - repeated execute returned `422 validation_failed`;
    - contact search found `Phase 6 Imported Contact 1778278900`;
    - account import regression job `ij_33f8c8828a03` still executed successfully and created account `acc_568ed53dfaea`.
- implemented the seventh `Phase 6` coding slice in code:
  - updated frontend import request typing to support `account` and `contact`;
  - added account/contact import mode selection to `BulkOperationsPanel`;
  - added contact import default CSV content and file name;
  - generated contact mapping for full name, email, phone and account name;
  - kept account import mapping and account export behavior intact;
  - row labels now display either account name or contact full name;
  - passed `docker compose exec -T frontend npm run build`;
  - verified backend `/readyz` on port `8081` and frontend dev server on port `5173`;
  - passed browser smoke on `2026-05-09`:
    - RevOps Admin selected contact import mode;
    - RevOps Admin previewed contact CSV through the UI;
    - RevOps Admin executed the contact import through the UI;
    - UI showed `executed`, `created`, imported contact name and `Contact full name is required`;
    - backend contact search found `Phase 6 UI Imported Contact 1778279300` as `con_a8428f131dcb`.
- implemented the eighth `Phase 6` coding slice in code:
  - added `V14__phase6_opportunity_export.sql` to allow `opportunity` export jobs;
  - extended export job request with optional `stageKey`;
  - extended `ExportJobService` to support `entityType = opportunity`;
  - reused `OpportunityRepository.listVisible` with the existing tenant/owner-scope filter shape;
  - supported opportunity export criteria: `query`, `ownerId`, `stageKey` and `limit`;
  - generated opportunity CSV with opportunity id, title, account, owner, stage, expected amount, close date and approval state;
  - preserved existing account export behavior;
  - passed `docker compose exec -T backend gradle compileKotlin --no-daemon`;
  - recreated backend service so Flyway picked up the new migration resource;
  - verified Flyway applied `V14` successfully and `/readyz` passed on port `8081`;
  - passed API smoke on `2026-05-09`:
    - RevOps Admin created account `acc_16d687ba4212`;
    - RevOps Admin created opportunity `opp_febd5bea0a5e`;
    - Sales Rep opportunity export returned `403 forbidden`;
    - Sales Manager opportunity export returned `403 forbidden`;
    - RevOps Admin created opportunity export job `ej_aecb94e1314b`;
    - opportunity export returned `completed` with `rowCount = 1`;
    - CSV contained `Phase 6 Opportunity Export 1778279600`, stage `qualification`, amount `123456.00`, close date `2026-07-15` and approval state `none`;
    - export job detail returned the persisted CSV content;
    - stage-filter mismatch export job `ej_4aa915a6c55c` returned `rowCount = 0`;
    - account export regression job `ej_ce71d1f7b66b` returned `rowCount = 1` for `Phase 6 Opportunity Export Account 1778279600`.
- implemented the ninth `Phase 6` coding slice in code:
  - updated frontend export request typing to support `account` and `opportunity`;
  - added account/opportunity export mode selection to `BulkOperationsPanel`;
  - added opportunity `stageKey` criteria input;
  - kept import controls, account export and CSV output display intact;
  - passed `docker compose exec -T frontend npm run build`;
  - verified backend `/readyz` on port `8081` and frontend dev server on port `5173`;
  - passed browser smoke on `2026-05-09`:
    - RevOps Admin selected opportunity export mode;
    - RevOps Admin exported `Phase 6 UI Opportunity Export 1778280300`;
    - UI showed completed export job with one row;
    - CSV output contained opportunity title, account name, amount `78901.00` and stage `qualification`.
- implemented the tenth `Phase 6` coding slice in code:
  - added `V15__phase6_opportunity_import.sql` to allow `opportunity` import jobs;
  - extended backend import preview supported entities, field inference and target fields for opportunities;
  - added opportunity row validation for title, account resolution, stage key existence, expected amount parsing and ISO close date parsing;
  - normalized preview data with resolved account id/name, stage key, expected amount and close date;
  - extended async import worker execution to create opportunities through `OpportunityRepository`;
  - preserved account and contact import behavior after the entity expansion;
  - passed `docker compose exec -T backend gradle compileKotlin --no-daemon`;
  - recreated backend service so Flyway picked up the new migration resource;
  - verified Flyway applied `V15` successfully and `/readyz` passed on port `8081`;
  - passed API smoke on `2026-05-09`:
    - RevOps Admin created account `acc_361012a8280b` for opportunity import resolution;
    - Sales Rep and Sales Manager opportunity import preview returned `403 forbidden`;
    - RevOps Admin created opportunity import preview job `ij_7e00580eca69`;
    - preview returned `entityType = opportunity`, `status = previewed`, `validRows = 1` and `invalidRows = 1`;
    - preview resolved account name to account id and preserved row-level errors `Opportunity title is required` and `Expected amount must be a number`;
    - Sales Rep and Sales Manager execute attempts returned `403 forbidden`;
    - RevOps Admin enqueued and executed the import job asynchronously;
    - final job status was `executed` with `executedRows = 1` and `skippedRows = 1`;
    - created opportunity `opp_e3a9026e80d1` was visible through opportunity search;
    - repeat execute returned `422`;
    - account import regression job `ij_58731096ed6c` executed with `1` created and `1` skipped row;
    - contact import regression job `ij_ba05b0a33611` executed with `1` created and `1` skipped row.
- implemented the eleventh `Phase 6` coding slice in code:
  - updated frontend import request typing to support `account`, `contact` and `opportunity`;
  - added opportunity import mode selection to `BulkOperationsPanel`;
  - added opportunity default CSV content and file name;
  - added opportunity import mapping for title, account name, stage key, expected amount and close date;
  - updated row labels to display opportunity title in preview/execution rows;
  - kept account/contact import modes and account/opportunity export modes intact;
  - passed `docker compose exec -T frontend npm run build`;
  - verified backend `/readyz` on port `8081` and frontend dev server on port `5173`;
  - passed browser smoke on `2026-05-09`:
    - RevOps Admin selected opportunity import mode;
    - RevOps Admin previewed `Phase 6 UI Imported Opportunity 1778280702093`;
    - UI showed row-level `Opportunity title is required` and `Expected amount must be a number`;
    - RevOps Admin executed the opportunity import through the UI;
    - UI showed `executed`, `created` and the imported opportunity title;
    - backend opportunity search found `Phase 6 UI Imported Opportunity 1778280702093` as `opp_29aa6508412d`;
    - Sales Rep and Sales Manager workspaces did not expose Data Operations controls;
    - account import UI regression created `Phase 6 UI Account Import Regression 1778280834262`;
    - contact import UI regression created `Phase 6 UI Contact Import Regression 1778280834262`.
- completed the `Phase 6` decision step:
  - accepted Phase 6 for the MVP path with deferred enhancements documented in `phase6_completion_decision_note.md`;
  - entered Phase 7 with `phase7_first_coding_slice_planning_note.md`.
- implemented the first `Phase 7` coding slice in code:
  - added `V16__phase7_duplicate_candidates.sql` with durable duplicate candidate storage;
  - added backend duplicate candidate DTOs, repository, service and controller under `dedup`;
  - added `POST /api/duplicate-candidates/generate` for RevOps Admin duplicate candidate generation;
  - added `GET /api/duplicate-candidates` for RevOps Admin review queue reads;
  - generated account candidates from exact normalized account name matches;
  - generated contact candidates from exact normalized contact email matches;
  - persisted candidate labels, match score and business-readable reason summary;
  - made repeated generation idempotent for existing pairs through a tenant/entity/pair uniqueness constraint;
  - restricted duplicate candidate generation/listing to RevOps Admin;
  - passed `docker compose exec -T backend gradle compileKotlin --no-daemon`;
  - recreated backend service so Flyway picked up the new migration resource;
  - verified Flyway applied `V16` successfully and `/readyz` passed on port `8081`;
  - passed API smoke on `2026-05-09`:
    - RevOps Admin created duplicate account pair `acc_32fc97e40726` / `acc_cef143e9ad51`;
    - RevOps Admin generated account duplicate candidate `dc_1c2e3aa745af` with score `90`;
    - repeated account generation returned `generatedCount = 0`;
    - RevOps Admin created duplicate contact pair `con_8ec51d10b370` / `con_e38c4f9e6568`;
    - RevOps Admin generated contact duplicate candidate `dc_1cbd1335a040` with score `95`;
    - duplicate candidate list returned both generated candidates with reason summaries;
    - Sales Rep and Sales Manager generate/list attempts returned `403 forbidden`;
    - unsupported `opportunity` generation returned `422 validation_failed`.
- implemented the second `Phase 7` coding slice in code:
  - added `V17__phase7_duplicate_candidate_rejection.sql` with `review_reason`;
  - added duplicate candidate rejection request/response support;
  - added tenant-scoped candidate lookup and open-candidate rejection update;
  - added `POST /api/duplicate-candidates/{candidateId}/reject`;
  - stored reviewer, reviewed timestamp and optional review reason;
  - kept rejected candidates out of the default open queue while preserving `status=rejected` reads;
  - preserved idempotent generation so rejected pairs are not recreated as open candidates;
  - passed `docker compose exec -T backend gradle compileKotlin --no-daemon`;
  - recreated backend service so Flyway picked up the new migration resource;
  - verified Flyway applied `V17` successfully and `/readyz` passed on port `8081`;
  - passed API smoke on `2026-05-09`:
    - RevOps Admin generated candidate `dc_be1e4de06e41`;
    - Sales Rep and Sales Manager reject attempts returned `403 forbidden`;
    - RevOps Admin rejected the candidate with reason `False positive rejection 1778281789983`;
    - second reject attempt returned `422 validation_failed`;
    - default open queue no longer included the rejected candidate;
    - `status=rejected` queue included the rejected candidate and review reason;
    - repeated generation did not reopen the rejected pair.
- implemented the third `Phase 7` coding slice in code:
  - added frontend duplicate candidate contracts in `types/duplicateCandidates.ts`;
  - added frontend duplicate candidate API wrappers in `api/duplicateCandidates.ts`;
  - added RevOps-only `DuplicateReviewPanel`;
  - wired duplicate review into the CRM workspace for RevOps Admin only;
  - supported account/contact candidate generation, open queue refresh and candidate rejection from the UI;
  - displayed candidate labels, match score and reason summary;
  - kept existing Data Operations import/export UI intact;
  - passed `docker compose exec -T frontend npm run build`;
  - verified backend `/readyz` on port `8081`;
  - recreated frontend service on the same port `5173` after host port probing stopped connecting, preserving the original port;
  - passed browser smoke on `2026-05-09`:
    - RevOps Admin saw Duplicate Review and Data Operations;
    - RevOps Admin generated account candidate `dc_d007eb3920de`;
    - RevOps Admin rejected the candidate with reason `UI false positive 1778282123229`;
    - backend rejected queue confirmed the persisted review reason;
    - Sales Rep and Sales Manager workspaces did not expose Duplicate Review controls.
- implemented the fourth `Phase 7` coding slice in code:
  - added `V18__phase7_account_duplicate_merge.sql` with duplicate candidate merge result fields;
  - added account duplicate merge request/response contracts;
  - added `POST /api/duplicate-candidates/{candidateId}/merge-account`;
  - validated candidate existence, `account` entity type, open status and master id membership in the candidate pair;
  - reassigned contacts from duplicate account to master account;
  - reassigned opportunities from duplicate account to master account;
  - marked the candidate `merged` with reviewer, master id, duplicate id and merge reason;
  - kept merged candidates out of the open queue while preserving `status=merged` reads;
  - passed `docker compose exec -T backend gradle compileKotlin --no-daemon`;
  - recreated backend service so Flyway picked up the new migration resource;
  - verified Flyway applied `V18` successfully and `/readyz` passed on port `8081`;
  - added runtime smoke scenario `phase7-account-merge` under the approved `npm run runtime:smoke` runner;
  - passed runtime smoke on `2026-05-09`:
    - RevOps Admin generated account candidate `dc_e3f963ec4427`;
    - Sales Rep and Sales Manager merge attempts returned `403 forbidden`;
    - invalid master id returned `422 validation_failed`;
    - RevOps Admin merged duplicate account `acc_d871be7f911c` into master account `acc_76097027bdf1`;
    - contact `con_80921a4c3967` and opportunity `opp_e5e03fd2383e` were reassigned to the master account;
    - second merge attempt returned `422 validation_failed`;
    - merged candidate moved from the open queue to the merged queue with persisted merge metadata.
- implemented the fifth `Phase 7` coding slice in code:
  - added contact duplicate merge request/response contracts;
  - added `POST /api/duplicate-candidates/{candidateId}/merge-contact`;
  - validated candidate existence, `contact` entity type, open status and master id membership in the candidate pair;
  - reassigned opportunities whose `primary_contact_id` pointed at the duplicate contact to the master contact;
  - marked the candidate `merged` with reviewer, master id, duplicate id and merge reason;
  - kept merged candidates out of the open queue while preserving `status=merged` reads;
  - passed `docker compose exec -T backend gradle compileKotlin --no-daemon`;
  - recreated backend service on the existing `8081` port;
  - verified `/readyz` passed on port `8081`;
  - added runtime smoke scenario `phase7-contact-merge` under the approved `npm run runtime:smoke` runner;
  - passed runtime smoke on `2026-05-09`:
    - RevOps Admin generated contact candidate `dc_b9b07ee03625`;
    - Sales Rep and Sales Manager merge attempts returned `403 forbidden`;
    - invalid master id returned `422 validation_failed`;
    - RevOps Admin merged duplicate contact `con_320623bffcaa` into master contact `con_90630318076e`;
    - opportunity `opp_8688c5c13ad6` primary contact was reassigned to the master contact;
    - second merge attempt returned `422 validation_failed`;
    - merged candidate moved from the open queue to the merged queue with persisted merge metadata.
- implemented the sixth `Phase 7` coding slice in code:
  - added frontend merge request/response typing for account and contact duplicate candidates;
  - added frontend API wrappers for `merge-account` and `merge-contact`;
  - updated `DuplicateReviewPanel` with master-record selection, merge reason input and merge action;
  - kept generate, refresh and reject behavior intact;
  - displayed merge result counts after successful merge;
  - updated the runtime smoke runner default frontend origin from `127.0.0.1:5173` to `localhost:5173` so browser tests match backend CORS;
  - added runtime smoke scenario `phase7-merge-ui`;
  - passed `docker compose exec -T frontend npm run build`;
  - passed `npm run runtime:smoke -- health`;
  - passed browser/runtime smoke on `2026-05-09`:
    - RevOps Admin merged account candidate `dc_f0aa366757c1` through the UI;
    - RevOps Admin merged contact candidate `dc_a9412dbb5788` through the UI;
    - backend merged queues confirmed persisted merge metadata;
    - UI reject flow still worked;
    - Sales Rep and Sales Manager workspaces did not expose Duplicate Review controls.
- implemented the seventh `Phase 7` coding slice in code:
  - added `V19__phase7_business_audit_events.sql` with tenant-scoped business audit events;
  - added backend audit DTOs, repository, service and controller under `audit`;
  - added `GET /api/business-audit-events` for RevOps Admin recent audit reads;
  - wrote account duplicate merge audit events with actor, candidate id, master id, duplicate id and reassigned relation counts;
  - wrote contact duplicate merge audit events with actor, candidate id, master id, duplicate id and reassigned primary-contact opportunity count;
  - restricted business audit event feed to RevOps Admin;
  - passed `docker compose exec -T backend gradle compileKotlin --no-daemon`;
  - recreated backend service so Flyway picked up the new migration resource;
  - verified Flyway applied `V19` successfully and `/readyz` passed on port `8081`;
  - added runtime smoke scenario `phase7-merge-audit`;
  - passed runtime smoke on `2026-05-09`:
    - account merge candidate `dc_515af6393694` wrote audit event `bae_55f36190eb4a`;
    - contact merge candidate `dc_fbb13c856acd` wrote audit event `bae_659fec08278d`;
    - audit details preserved master id, duplicate id and reassigned counts;
    - Sales Rep and Sales Manager audit feed reads returned `403 forbidden`.

## Deferred Items

- add custom field list columns if needed.
- migrate away from the current legacy `opportunities.stage_id` persistence bridge when schema/data cleanup is worth the migration risk.

## Current Focus

Next planned implementation step:

```text
next deployment maturity decision: select actual secret provider or run real external staging execution
```

## Verification Gap

- backend compile verification now passes inside the backend container;
- host-level `gradle` command is not available in the current environment;
- frontend build verification passes inside the frontend container;
- `V2` migration was executed successfully in the disposable smoke runtime;
- clean restart verification passed on `2026-05-08` without deleting named volumes;
- `V3` migration and account endpoints have passed a narrow runtime sanity check;
- contact endpoints have passed a narrow runtime sanity check;
- opportunity endpoints have passed a narrow runtime sanity check;
- activity endpoints have passed a narrow runtime sanity check;
- the move-stage command has passed a narrow runtime sanity check;
- the opportunity patch/update baseline has passed a narrow runtime sanity check;
- the opportunity reassign-owner command has passed a narrow runtime sanity check;
- the manager/team opportunity scope baseline has passed a narrow runtime sanity check;
- the frontend CRM read shell has passed a narrow build/dev sanity check;
- the frontend CRM create forms have passed a narrow build/API sanity check;
- the frontend opportunity action controls have passed a narrow build/API sanity check;
- the frontend activity section has passed a narrow build/API sanity check;
- broader Phase 2 API/build/dev verification passed on the current running compose stack on `2026-05-08`;
- browser-level Sales Rep CRM create/action/session verification passed on `2026-05-08`;
- manager, RevOps and approver Phase 2 coverage is currently API-level, not a full browser-per-persona walkthrough;
- Phase 3 `V4` approval core migration passed on the current running compose stack on `2026-05-08`;
- Phase 3 approval DTO/repository backend baseline compiles on `2026-05-08`;
- Phase 3 approval policy/state/visibility backend baseline compiles on `2026-05-08`;
- Phase 3 approval service/bridge baseline compiles and starts after backend restart on `2026-05-08`;
- Phase 3 backend approval endpoints passed a narrow API sanity check on `2026-05-08`;
- Phase 3 frontend approval API/types passed build/API sanity checks on `2026-05-08`;
- Phase 3 frontend opportunity approval submit strip passed build/browser sanity checks on `2026-05-08`;
- Phase 3 frontend approver inbox passed build/API/browser sanity checks on `2026-05-08`;
- Phase 3 frontend approval detail/decision UI passed build/API/browser sanity checks on `2026-05-08`;
- Phase 3 final Legal Approver approval and Sales Rep outcome browser verification passed on `2026-05-08`;
- Phase 4 `V5` metadata core migration passed on the current running compose stack on `2026-05-08`;
- Phase 4 first backend metadata schema foundation compiles and is query-verified on `2026-05-08`;
- Phase 4 backend metadata DTO/repository read baseline compiles and starts on `2026-05-08`.
- Phase 4 backend draft/publish validation service compiles and starts on `2026-05-08`.
- Phase 4 backend metadata admin endpoints compile, start and pass API smoke on `2026-05-08`.
- Phase 4 frontend metadata admin read/create draft UI builds and passes browser smoke on `2026-05-08`.
- Phase 4 runtime published metadata read path compiles, starts and passes API smoke on `2026-05-08`.
- Phase 4 metadata-aware CRM forms/detail display builds and passes browser smoke on `2026-05-08`.
- Phase 4 metadata-aware stage validation compiles, starts and passes API smoke on `2026-05-08`.
- Phase 4 metadata admin draft edit endpoints compile, start and pass API smoke on `2026-05-08`.
- Phase 4 custom field value capture/rendering compiles, builds and passes API/browser smoke on `2026-05-08`.
- Phase 4 frontend metadata admin field/stage edit UI builds and passes browser smoke on `2026-05-08`.
- Phase 4 published metadata config rollback/new-draft management compiles, builds and passes reversible API smoke on `2026-05-08`.
- Headless Chrome DOM smoke can load the frontend shell, but in the current sandbox it reports `Failed to fetch` for demo-user API calls; this pass was therefore accepted on API/build verification rather than browser-level UI verification.
- Phase 4 opportunity read joins replaced with published metadata stage runtime compiles and passes API smoke on `2026-05-08`.
- Phase 4 completion review is a documentation/status decision; no runtime verification was needed for that artifact-only update.
- Phase 5 first saved-view baseline compiles, builds, applies `V6`, restarts and passes API smoke on `2026-05-08`.
- Phase 5 second saved-view management baseline compiles, builds, applies `V7`, restarts and passes API smoke on `2026-05-08`.
- Phase 5 third custom-field saved-view filter execution compiles, builds, restarts and passes API smoke on `2026-05-08`.
- Phase 5 fourth manager visibility persistence compiles, applies `V8`, restarts and passes API smoke on `2026-05-08`.
- Phase 5 fifth manager account/contact workspace scope alignment compiles, restarts and passes API smoke on `2026-05-08`.
- Phase 5 sixth access regression and frontend empty-state refinement builds and passes API smoke on `2026-05-08`.
- Phase 5 completion review is a documentation/status decision; no runtime verification was needed for that artifact-only update.
- Phase 6 first import preview baseline compiles, applies `V9`, restarts and passes API smoke on `2026-05-08`.
- Phase 6 second account import execution baseline compiles, applies `V10`, restarts and passes API smoke on `2026-05-08`.
- Phase 6 third controlled export job baseline compiles, applies `V11`, restarts and passes API smoke on `2026-05-08`.
- Phase 6 fourth async import execution worker baseline compiles, applies `V12`, restarts and passes API smoke on `2026-05-09`.
- Phase 6 fifth import/export frontend operations baseline builds and passes browser smoke on `2026-05-09`.
- Phase 6 sixth contact import baseline compiles, applies `V13`, restarts and passes API smoke on `2026-05-09`.
- Phase 6 seventh contact import frontend support baseline builds and passes browser smoke on `2026-05-09`.
- Phase 6 eighth opportunity export baseline compiles, applies `V14`, restarts and passes API smoke on `2026-05-09`.
- Phase 6 ninth opportunity export frontend support baseline builds and passes browser smoke on `2026-05-09`.
- Phase 6 tenth opportunity import baseline compiles, applies `V15`, restarts and passes API smoke on `2026-05-09`.
- Phase 6 eleventh opportunity import frontend support baseline builds and passes browser smoke on `2026-05-09`.
- Phase 6 completion decision is a documentation/status decision; no runtime verification was needed for that artifact-only update.
- Phase 7 first duplicate candidate storage/generation baseline compiles, applies `V16`, restarts and passes API smoke on `2026-05-09`.
- Phase 7 second duplicate false-positive rejection baseline compiles, applies `V17`, restarts and passes API smoke on `2026-05-09`.
- Phase 7 third duplicate review frontend baseline builds and passes browser smoke on `2026-05-09`.
- Phase 7 fourth account duplicate merge backend baseline compiles, applies `V18`, restarts and passes runtime smoke on `2026-05-09`.
- Phase 7 fifth contact duplicate merge backend baseline compiles, restarts and passes runtime smoke on `2026-05-09`.
- Phase 7 sixth duplicate merge frontend controls baseline builds and passes browser/runtime smoke on `2026-05-09`.
- Phase 7 seventh merge audit event baseline compiles, applies `V19`, restarts and passes runtime smoke on `2026-05-09`.
- Phase 8 first reporting projection foundation baseline compiles, applies `V20`, restarts and passes runtime smoke on `2026-05-09`.
- Phase 8 second reporting dashboard frontend baseline builds and passes browser/runtime smoke on `2026-05-09`.
- Phase 8 third reporting drill-down backend baseline compiles, restarts and passes runtime smoke on `2026-05-09`.
- Phase 8 fourth reporting dashboard drill-down frontend controls baseline builds and passes browser/runtime smoke on `2026-05-09`.
- Phase 8 completion decision is a documentation/status decision; no runtime verification was needed for that artifact-only update.
- Phase 9 first pilot hardening audit/checklist baseline is a documentation/status slice; no runtime verification was needed for that artifact-only update.
- Phase 9 second end-to-end pilot runtime smoke baseline passes on `2026-05-09`.
- Phase 9 third metadata publish safety runtime smoke baseline passes on `2026-05-09`.
- Phase 9 fourth approval negative-path runtime smoke baseline passes on `2026-05-09`.
- Phase 9 completion decision is a documentation/status decision; no runtime verification was needed for that artifact-only update.
- MVP pilot full smoke gate suite rerun passed on `2026-05-09`:
  - `health`;
  - `phase9-pilot-e2e`;
  - `phase9-metadata-safety`;
  - `phase9-approval-negative`;
  - `phase8-reporting-foundation`;
  - `phase8-reporting-drilldown`;
  - `phase8-reporting-ui`;
  - `phase8-reporting-drilldown-ui`.
- MVP pilot smoke suite orchestration passed through `npm run pilot:smoke` on `2026-05-09`; the suite ran all accepted pilot gates and closed the Chrome process it started.
- MVP pilot contact/opportunity import matrix smoke passed on `2026-05-09`; `npm run pilot:smoke` now includes `phase9-import-matrix` and passed with the expanded gate suite.
- MVP pilot metadata rollback smoke passed on `2026-05-09`; `npm run pilot:smoke` now includes `phase9-metadata-rollback` and passed with the expanded gate suite.
- MVP pilot CI smoke workflow baseline passed on `2026-05-09`; frontend build and `npm run pilot:smoke` passed after adding the CI entrypoint/workflow and Chrome binary override.
- MVP pilot production deployment planning note drafted on `2026-05-09`; no runtime verification was needed for this planning-only update.
- MVP pilot production deployment packaging baseline passed on `2026-05-09`:
  - production compose config validation passed;
  - existing dev compose config validation still passed;
  - `scripts/core/deployment-smoke.sh` syntax validation passed;
  - production frontend image built through `npm ci` and `npm run build`;
  - production backend image built through `gradle bootJar --no-daemon`;
  - deployment health smoke passed against the running local runtime after sandbox localhost permission was granted;
  - production compose runtime was not started because the existing dev stack already occupied ports `8081` and `5173`.
- MVP pilot explicit migration command passed on `2026-05-10`:
  - `scripts/core/production-migrate.sh` syntax validation passed;
  - production compose config validation passed with the `migrate` service included;
  - missing env file guard returned a clear failure instead of using `.env.production.example`;
  - isolated migration run under Compose project `salesops-migrate-check` built the backend image, waited for PostgreSQL health, validated 20 migrations and exited successfully;
  - dev runtime readiness and deployment health smoke still passed after the isolated migration verification.
- MVP pilot backup/restore drill passed on `2026-05-10`:
  - `scripts/core/production-backup.sh` and `scripts/core/production-restore-drill.sh` syntax validation passed;
  - missing env/backup guard checks returned clear failures;
  - isolated source database under Compose project `salesops-backup-source` applied all 20 migrations;
  - PostgreSQL custom-format dump `/tmp/salesops-backup-source.dump` was produced;
  - dump restored into separate Compose project `salesops-restore-drill-check`;
  - restored database reported latest successful Flyway version `20`, 20 successful Flyway records and tenant seed count `1`;
  - disposable backup source and restore drill stacks were removed with volumes;
  - dev runtime readiness and deployment health smoke still passed after cleanup.
- MVP pilot rollback dry run passed on `2026-05-10`:
  - rollback drill script syntax validation passed;
  - rollback previous/candidate compose config validation passed;
  - previous and candidate backend/frontend images built with separate tags;
  - isolated production Compose stack started on backend port `18081` and frontend port `15173`;
  - previous image set passed backend/frontend health;
  - candidate image set passed backend/frontend health;
  - rollback to previous image set passed backend/frontend health;
  - disposable rollback drill stack and volume were removed;
  - dev runtime readiness and deployment health smoke still passed after cleanup.
- MVP pilot staging/secrets baseline passed on `2026-05-10`:
  - `scripts/checks/validate-deploy-env.sh` syntax validation passed;
  - `.env.staging.example` and `.env.production.example` correctly failed validation because they contain placeholder secrets;
  - sanitized staging env `/tmp/salesops-staging-valid.env` passed validation;
  - production compose config resolved successfully against the sanitized staging env;
  - tools profile still exposed `db`, `backend`, `frontend` and `migrate`;
  - dev runtime readiness still passed after validation.
- MVP pilot external staging handoff passed on `2026-05-10`:
  - `scripts/checks/staging-handoff-check.sh` syntax validation passed;
  - required staging handoff files were present;
  - deploy scripts were executable;
  - `.env.staging.example` correctly failed validation because it contains placeholder secrets;
  - a temporary sanitized staging env passed validation;
  - production compose config resolved successfully from the sanitized env;
  - no handoff-check Compose stack was left running;
  - dev runtime readiness still passed after validation.
- MVP pilot managed secrets planning passed on `2026-05-10`:
  - `scripts/checks/validate-managed-secrets-plan.sh` syntax validation passed;
  - `.env.staging.example` correctly failed managed secret validation because mapped `POSTGRES_PASSWORD` is still a placeholder;
  - sanitized staging env `/tmp/salesops-staging-valid.env` passed managed secret mapping validation with one mapped secret;
  - staging handoff check still passed after adding managed secret mapping coverage;
  - dev runtime readiness still passed after validation.
- MVP pilot production platform/IaC planning accepted on `2026-05-10`:
  - added provider-neutral platform/IaC boundary in `codebase/deploy/PRODUCTION_PLATFORM_IAC.md`;
  - documented supported maturity paths: single-node Compose with host IaC, managed container platform, and Kubernetes only when operationally justified;
  - kept provider-specific IaC, Kubernetes manifests and cloud resources out of scope until a concrete target platform is selected;
  - no runtime verification was required for this documentation-only planning slice.
- MVP pilot single-node host IaC baseline passed on `2026-05-10`:
  - added `codebase/deploy/SINGLE_NODE_HOST_IAC.md`;
  - added `codebase/scripts/checks/host-preflight-check.sh`;
  - updated staging handoff coverage so the host baseline and preflight script are part of the external package;
  - verified the preflight script through local syntax and host sanity checks with a port-check override because the active dev runtime owns the default pilot ports;
  - dev runtime readiness still passed after validation.
- MVP pilot reverse proxy/TLS handoff passed on `2026-05-10`:
  - added `codebase/deploy/REVERSE_PROXY_TLS_HANDOFF.md`;
  - added `codebase/scripts/checks/reverse-proxy-tls-check.sh`;
  - updated deployment and external staging handoff docs so external route validation is part of acceptance;
  - verified the route check against the active local runtime with `REVERSE_PROXY_TLS_ALLOW_INSECURE=1`;
  - dev runtime readiness still passed after validation.
- MVP pilot image registry promotion baseline passed on `2026-05-10`:
  - added `codebase/deploy/IMAGE_REGISTRY_PROMOTION.md`;
  - added `codebase/deploy/image-promotion.manifest.example`;
  - added `codebase/scripts/checks/validate-image-promotion.sh`;
  - updated external staging handoff so image promotion validation runs before migration/startup;
  - validated staging example image refs and local production example image refs with explicit local allowance;
  - staging handoff check still passed after adding image promotion validation;
  - dev runtime readiness still passed after validation.
- MVP pilot CI release automation baseline passed on `2026-05-10`:
  - added `.github/workflows/release-build.yml`;
  - added `codebase/deploy/CI_RELEASE_AUTOMATION.md`;
  - added `codebase/scripts/ci/ci-release-build.sh`;
  - release build renders a validation env and image promotion manifest;
  - release build validates deploy env and image refs, resolves production Compose config, builds backend/frontend production images and inspects the resulting image refs;
  - staging handoff check still passed after adding CI release files;
  - dev runtime readiness still passed after validation.
- MVP pilot registry push automation baseline passed on `2026-05-10`:
  - added `codebase/deploy/REGISTRY_PUSH_AUTOMATION.md`;
  - added `codebase/scripts/ci/ci-registry-push.sh`;
  - updated `.github/workflows/release-build.yml` with a manual `push_images` input and dry-run default;
  - registry push entrypoint validates the image promotion manifest and only pushes when `CI_REGISTRY_PUSH_ENABLED=1`;
  - staging handoff check still passed after adding registry push files;
  - dev runtime readiness still passed after validation.
- MVP pilot staging manifest consumption passed on `2026-05-10`:
  - added `codebase/deploy/STAGING_MANIFEST_CONSUMPTION.md`;
  - added `codebase/scripts/env/render-deploy-env-from-manifest.sh`;
  - render script preserves protected base env values and replaces only `BACKEND_IMAGE`/`FRONTEND_IMAGE` from the retained release manifest;
  - rendered deploy env passed deploy env, managed secret and image promotion validation;
  - staging handoff check still passed after adding manifest consumption files;
  - dev runtime readiness still passed after validation.
- MVP pilot staging deploy automation passed on `2026-05-10`:
  - added `codebase/deploy/STAGING_DEPLOY_AUTOMATION.md`;
  - added `codebase/scripts/orchestration/staging-deploy.sh`;
  - staging deploy script validates the rendered env, managed secret mapping, image promotion refs and production Compose config;
  - apply mode is explicit through `STAGING_DEPLOY_APPLY=1` and runs host preflight, migration, Compose startup, reverse proxy/TLS check and deployment smoke;
  - dry-run passed against `/tmp/salesops-rendered-staging.env`;
  - staging handoff check still passed after adding deploy automation files;
  - dev runtime readiness still passed after validation.
- MVP pilot staging deploy apply drill passed on `2026-05-10`:
  - added `codebase/deploy/STAGING_DEPLOY_APPLY_DRILL.md`;
  - added `codebase/scripts/drills/staging-deploy-apply-drill.sh`;
  - drill runs `scripts/orchestration/staging-deploy.sh` in apply mode against isolated local ports and a disposable Compose project;
  - drill covers host preflight, explicit migration, Compose startup, reverse proxy/TLS route check and deployment health smoke;
  - disposable drill stack is removed after completion;
  - staging handoff check still passed after adding apply drill files;
  - dev runtime readiness still passed after validation.
- MVP pilot staging post-deploy gates passed on `2026-05-10`:
  - added `codebase/deploy/STAGING_POST_DEPLOY_GATES.md`;
  - added `codebase/scripts/orchestration/staging-post-deploy-gates.sh`;
  - post-deploy gate script validates deploy env, managed secret mapping, image refs, Compose config and backup target;
  - apply mode is explicit through `STAGING_POST_DEPLOY_APPLY=1` and runs route check, deployment health smoke, backup, restore drill and rollback dry run;
  - dry-run passed against `/tmp/salesops-rendered-staging.env`;
  - staging handoff check still passed after adding post-deploy gate files;
  - dev runtime readiness still passed after validation.
- MVP pilot staging post-deploy apply drill passed on `2026-05-10`:
  - added `codebase/deploy/STAGING_POST_DEPLOY_APPLY_DRILL.md`;
  - added `codebase/scripts/drills/staging-post-deploy-apply-drill.sh`;
  - updated `codebase/scripts/orchestration/staging-post-deploy-gates.sh` so backup output parsing is resilient to Docker Compose progress lines;
  - updated `codebase/scripts/core/production-restore-drill.sh` so restore drills tolerate an already-created target database before `pg_restore`;
  - drill runs deploy apply mode, route check, deployment health smoke, backup, restore drill and rollback dry run against isolated local projects;
  - disposable staging and restore/rollback drill stacks are cleaned up after completion;
  - staging handoff check still passed after adding post-deploy apply drill files;
  - dev runtime readiness still passed after validation.
- MVP pilot production readiness summary accepted on `2026-05-10`:
  - added `codebase/deploy/PRODUCTION_READINESS_SUMMARY.md`;
  - summarized current state as `staging-package-ready; production-readiness-blocked`;
  - documented verified local deployment gates and remaining production blockers;
  - identified external staging acceptance and provider-specific secret integration as next deployment maturity options;
  - staging handoff check still passed after adding readiness summary coverage;
  - dev runtime readiness still passed after validation.
- MVP pilot external staging acceptance run passed on `2026-05-10`:
  - added `codebase/deploy/EXTERNAL_STAGING_ACCEPTANCE.md`;
  - added `codebase/scripts/orchestration/external-staging-acceptance.sh`;
  - acceptance script runs handoff validation, env validation, managed secret mapping validation, image promotion validation, staging deploy dry-run and post-deploy dry-run;
  - acceptance script writes an acceptance report under `codebase/build/external-staging-acceptance/`;
  - dry-run passed against `/tmp/salesops-rendered-staging.env`;
  - staging handoff check still passed after adding acceptance files;
  - dev runtime readiness still passed after validation.
- MVP pilot secret provider adapter contract passed on `2026-05-10`:
  - added `codebase/deploy/SECRET_PROVIDER_ADAPTERS.md`;
  - added `codebase/scripts/env/render-env-from-secret-provider.sh`;
  - adapter contract supports `env` and `dotenv` provider output shapes without selecting a cloud/SaaS provider;
  - dotenv adapter rendered a deploy env from `.env.staging.example` and `/tmp/salesops-rendered-staging.env`;
  - env adapter rendered a deploy env from `.env.staging.example` and process environment input;
  - both rendered env files passed deploy env validation and managed secret mapping validation;
  - external staging acceptance dry-run passed using `/tmp/salesops-secret-provider-dotenv.env`.

## Rule

Не переходить к архитектуре, стеку и коду до завершения product design chain в правильном порядке.
