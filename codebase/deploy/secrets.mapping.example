# Provider-neutral mapping from deployment env keys to managed secret references.
# The right-hand side is intentionally not consumed by local scripts yet.
# Operators should bind each reference to their chosen secret manager.

POSTGRES_PASSWORD=secret://salesops/staging/postgres-password

