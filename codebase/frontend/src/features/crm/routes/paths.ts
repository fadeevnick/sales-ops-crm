export function buildOpportunityPath(opportunityId: string): string {
  return `/opportunities/${opportunityId}`;
}

export function buildAccountPath(accountId: string): string {
  return `/accounts/${accountId}`;
}
